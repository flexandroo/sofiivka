/* Generated by scripts/build-catalog-data.mjs. Edit catalog/*.js source modules instead. */
/* Source: catalog/categories.js */
(function () {
  "use strict";

  const category = config => ({
    status: "active",
    visibility: "catalog",
    menuDescription: "",
    allowedFacetIds: [],
    seo: {},
    ...config
  });

  const pumpFacets = ["productType", "type", "headM", "flowM3h", "powerKw", "connection", "pressureBar", "voltage", "protectionClass", "control"];
  const boilerFacets = ["productType", "type", "heatOutputKw", "fuel", "efficiencyPercent", "waterVolumeL", "pressureBar", "maxWaterTemperatureC", "chimneyDiameterMm", "weightKg"];
  const automationFacets = ["productType", "type", "control", "communication", "zones", "installation", "voltage", "protectionClass", "temperature"];
  const hydraulicFacets = ["productType", "type", "outlets", "connection", "flowM3h", "heatOutputKw", "material", "pressureBar"];
  const accessoryFacets = ["productType", "type", "compatibility", "connection", "material", "voltage", "consumptionPowerW", "weightKg"];
  const treatmentFacets = ["productType", "purpose", "waterSource", "technology", "installation", "capacityLh", "waterType", "format", "pump", "mineralizer", "flowType", "scope"];

  const definitions = [
    category({ id: "heating", slug: "heating", parentId: null, level: 1, title: "Опалення", shortTitle: "Опалення", description: "Обладнання для генерації, розподілу й керування теплом у житлових, комерційних та промислових системах.", menuDescription: "Котли, теплові насоси, гідравліка та автоматика", order: 10, seo: { title: "Опалення | ТД «Софіївка»", description: "Котли, теплові насоси, гідравліка, автоматика та комплектуючі для систем опалення." } }),
    category({ id: "water-supply", slug: "water-supply", parentId: null, level: 1, title: "Водопостачання та каналізація", shortTitle: "Вода й каналізація", description: "Насоси, установки, баки, автоматика та комплектуючі для подачі й відведення води.", menuDescription: "Насоси, каналізаційні установки, баки й автоматика", order: 20, seo: { title: "Водопостачання та каналізація | ТД «Софіївка»", description: "Насоси та обладнання для водопостачання, підвищення тиску й каналізації." } }),
    category({ id: "water-treatment", slug: "water-treatment", parentId: null, level: 1, title: "Водоочищення", shortTitle: "Водоочищення", description: "Питні фільтри, системи очищення всього будинку, картриджі, матеріали та UV-C знезараження.", menuDescription: "Фільтри, осмос, пом’якшення, картриджі та UV-C", order: 30, allowedFacetIds: treatmentFacets, seo: { title: "Водоочищення | ТД «Софіївка»", description: "Системи водоочищення, питні фільтри, картриджі, матеріали й UV-C знезараження." } }),
    category({ id: "smart-home", slug: "smart-home", parentId: null, level: 1, title: "Розумний будинок і автоматизація", shortTitle: "Розумний будинок", description: "Центральні блоки, освітлення, реле, датчики, захист від протікання та керування кліматом.", menuDescription: "Sinum, освітлення, реле, датчики та клімат", order: 40, seo: { title: "Розумний будинок і автоматизація | ТД «Софіївка»", description: "Компоненти TECH Sinum для автоматизації будинку, освітлення, клімату та безпеки." } }),
    category({ id: "climate", slug: "climate", parentId: null, level: 1, title: "Клімат", shortTitle: "Клімат", description: "Кондиціонери та системи туманоутворення для охолодження й зволоження повітря.", menuDescription: "Кондиціонери та туманоутворення", order: 50, seo: { title: "Кліматичне обладнання | ТД «Софіївка»", description: "Кондиціонери та системи туманоутворення для житлових і комерційних об’єктів." } }),
    category({ id: "household-equipment", slug: "household-equipment", parentId: null, level: 1, title: "Обладнання для господарства", shortTitle: "Для господарства", description: "Електричне обладнання для підготовки кормів та повсякденних господарських задач.", menuDescription: "Подрібнення та підготовка кормів", order: 60, seo: { title: "Обладнання для господарства | ТД «Софіївка»", description: "Електричне обладнання для приватного та фермерського господарства." } }),
    category({ id: "plumbing", slug: "plumbing", parentId: null, level: 1, title: "Сантехніка", shortTitle: "Сантехніка", description: "Напрям готується до наповнення перевіреним асортиментом.", menuDescription: "Рішення під замовлення", order: 70, status: "future", visibility: "landing", seo: { title: "Сантехніка | ТД «Софіївка»", description: "Сантехнічні рішення під замовлення з підбором і комплектацією." } }),

    category({ id: "heat-generation", slug: "heat-generation", parentId: "heating", level: 2, title: "Теплогенерація", shortTitle: "Теплогенерація", description: "Котли, пальники, теплогенератори та парогенератори за типом палива й призначенням.", order: 110 }),
    category({ id: "gas-boilers", slug: "gas-boilers", parentId: "heat-generation", level: 3, title: "Газові котли", shortTitle: "Газові котли", description: "Побутові, комерційні та промислові газові котли.", order: 111, allowedFacetIds: ["productType", "type", "heatOutputKw", "efficiencyPercent", "dhwFlowLMin", "installation", "energyClass", "dimensions", "weightKg", "voltage", "protectionClass"] }),
    category({ id: "solid-fuel-boilers", slug: "solid-fuel-boilers", parentId: "heat-generation", level: 3, title: "Твердопаливні котли", shortTitle: "Твердопаливні котли", description: "Котли з ручним завантаженням дров, вугілля та брикетів.", order: 112, allowedFacetIds: boilerFacets }),
    category({ id: "pellet-boilers", slug: "pellet-boilers", parentId: "heat-generation", level: 3, title: "Пелетні котли й мінікотельні", shortTitle: "Пелетні котли", description: "Автоматизовані пелетні котли та готові мінікотельні.", order: 113, allowedFacetIds: boilerFacets }),
    category({ id: "pellet-burners", slug: "pellet-burners", parentId: "heat-generation", level: 3, title: "Пелетні пальники", shortTitle: "Пелетні пальники", description: "Пелетні пальники для модернізації сумісного котельного обладнання.", order: 114, allowedFacetIds: accessoryFacets }),
    category({ id: "heat-generators", slug: "heat-generators", parentId: "heat-generation", level: 3, title: "Теплогенератори", shortTitle: "Теплогенератори", description: "Промислові повітряні теплогенератори для обігріву великих приміщень.", order: 115, allowedFacetIds: boilerFacets }),
    category({ id: "steam-generators", slug: "steam-generators", parentId: "heat-generation", level: 3, title: "Парогенератори", shortTitle: "Парогенератори", description: "Пелетні парогенератори для технологічних і промислових задач.", order: 116, allowedFacetIds: ["productType", "heatOutputKw", "fuel", "capacityKgh", "efficiencyPercent", "consumptionPowerW", "pressureBar", "weightKg"] }),

    category({ id: "heat-pumps", slug: "heat-pumps", parentId: "heating", level: 2, title: "Теплові насоси", shortTitle: "Теплові насоси", description: "Повітряні та геотермальні теплові насоси для опалення, охолодження й ГВП.", order: 120, allowedFacetIds: ["productType", "heatOutputKw", "coolingCapacityKw", "refrigerant", "cop", "scop", "energyClass", "voltage", "soundLevelDb", "maxWaterTemperatureC", "dimensions", "weightKg"] }),
    category({ id: "hot-water-storage", slug: "hot-water-storage", parentId: "heating", level: 2, title: "Гаряча вода й акумулювання", shortTitle: "ГВП і акумулювання", description: "Бойлери непрямого нагріву та буферні ємності.", order: 130 }),
    category({ id: "hot-water-tanks", slug: "hot-water-tanks", parentId: "hot-water-storage", level: 3, title: "Бойлери непрямого нагріву", shortTitle: "Бойлери", description: "Баки й бойлери для накопичення та приготування гарячої води.", order: 131, allowedFacetIds: ["productType", "volumeL", "heatOutputKw", "pressureBar", "maxWaterTemperatureC", "connection", "material", "installation", "dimensions", "weightKg", "compatibility"] }),
    category({ id: "heat-accumulators", slug: "heat-accumulators", parentId: "hot-water-storage", level: 3, title: "Буферні ємності й теплоакумулятори", shortTitle: "Теплоакумулятори", description: "Буферні ємності для акумулювання тепла та стабілізації системи.", order: 132, allowedFacetIds: ["productType", "volumeL", "pressureBar", "connection", "material", "temperature", "widthMm", "heightMm", "weightKg"] }),

    category({ id: "circulation-pumps", slug: "circulation-pumps", parentId: "heating", level: 2, title: "Насоси опалення", shortTitle: "Насоси опалення", description: "Циркуляційні насоси для опалення та рециркуляції гарячої води.", order: 140, allowedFacetIds: pumpFacets }),
    category({ id: "system-circulation-pumps", slug: "system-circulation", parentId: "circulation-pumps", level: 3, title: "Системні циркуляційні насоси", shortTitle: "Системна циркуляція", description: "Насоси для котлових, радіаторних, змішувальних та інших опалювальних контурів.", order: 141, allowedFacetIds: pumpFacets }),
    category({ id: "dhw-circulation-pumps", slug: "dhw-recirculation", parentId: "circulation-pumps", level: 3, title: "Насоси рециркуляції ГВП", shortTitle: "Рециркуляція ГВП", description: "Компактні насоси для контурів рециркуляції гарячої води.", order: 142, allowedFacetIds: pumpFacets }),

    category({ id: "distribution-hydraulics", slug: "distribution-hydraulics", parentId: "heating", level: 2, title: "Гідравліка та розподіл", shortTitle: "Гідравліка", description: "Насосні групи, колектори, гідравлічні роздільники, сепаратори й арматура.", order: 150, allowedFacetIds: hydraulicFacets }),
    category({ id: "pump-groups", slug: "pump-groups", parentId: "distribution-hydraulics", level: 3, title: "Насосні групи", shortTitle: "Насосні групи", description: "Готові модулі циркуляції та змішування для котельних контурів.", order: 151, allowedFacetIds: hydraulicFacets }),
    category({ id: "distribution-manifolds", slug: "distribution-manifolds", parentId: "distribution-hydraulics", level: 3, title: "Розподільчі колектори", shortTitle: "Колектори", description: "Колектори для розподілу теплоносія між контурами.", order: 152, allowedFacetIds: hydraulicFacets }),
    category({ id: "manifolds-with-hydraulic-separator", slug: "manifolds-with-separator", parentId: "distribution-hydraulics", level: 3, title: "Колектори з гідророздільником", shortTitle: "Колектори з гідрострілкою", description: "Комбіновані колектори з вбудованим гідравлічним розділенням.", order: 153, allowedFacetIds: hydraulicFacets }),
    category({ id: "hydraulic-separators", slug: "hydraulic-separators", parentId: "distribution-hydraulics", level: 3, title: "Гідравлічні роздільники", shortTitle: "Гідророздільники", description: "Гідрострілки та вузли гідравлічного розділення контурів.", order: 154, allowedFacetIds: hydraulicFacets }),
    category({ id: "air-dirt-separators", slug: "air-dirt-separators", parentId: "distribution-hydraulics", level: 3, title: "Сепаратори повітря та бруду", shortTitle: "Сепаратори", description: "Сепаратори для видалення повітря, шламу та магнітних домішок.", order: 155, allowedFacetIds: hydraulicFacets }),
    category({ id: "heating-valves", slug: "valves", parentId: "distribution-hydraulics", level: 3, title: "Змішувальна й балансувальна арматура", shortTitle: "Арматура", description: "Клапани, приводи та балансувальна арматура для гідравлічних контурів.", order: 156, allowedFacetIds: hydraulicFacets }),

    category({ id: "underfloor-heating", slug: "underfloor-heating", parentId: "heating", level: 2, title: "Тепла підлога", shortTitle: "Тепла підлога", description: "Колектори та вузли розподілу контурів водяної теплої підлоги.", order: 160, allowedFacetIds: ["productType", "outlets", "connection", "material", "pressureBar"] }),
    category({ id: "automation", slug: "automation", parentId: "heating", level: 2, title: "Автоматика опалення", shortTitle: "Автоматика опалення", description: "Термостати, контролери, зональне керування, датчики та комунікаційні модулі для систем опалення.", order: 170, allowedFacetIds: automationFacets }),
    category({ id: "room-thermostats", slug: "room-thermostats", parentId: "automation", level: 3, title: "Кімнатні термостати й програматори", shortTitle: "Термостати", description: "Кімнатне керування температурою, розкладом і режимами опалення.", order: 171, allowedFacetIds: automationFacets }),
    category({ id: "heating-controllers", slug: "system-controllers", parentId: "automation", level: 3, title: "Контролери котлів і систем", shortTitle: "Контролери", description: "Контролери котлів, насосів, клапанів і комплексних систем опалення.", order: 172, allowedFacetIds: automationFacets }),
    category({ id: "zone-control", slug: "zone-control", parentId: "automation", level: 3, title: "Зональне керування й приводи", shortTitle: "Зональне керування", description: "Контролери зон, термостатичні приводи та модулі теплої підлоги.", order: 173, allowedFacetIds: automationFacets }),
    category({ id: "heating-sensors-modules", slug: "sensors-modules", parentId: "automation", level: 3, title: "Датчики та комунікаційні модулі", shortTitle: "Датчики й модулі", description: "Датчики, ретранслятори, мережеві модулі та допоміжні компоненти автоматики.", order: 174, allowedFacetIds: automationFacets }),

    category({ id: "solar-thermal", slug: "solar-thermal", parentId: "heating", level: 2, title: "Сонячна теплова енергія", shortTitle: "Геліосистеми", description: "Сонячні колектори, баки та компоненти геліосистем.", order: 180, allowedFacetIds: ["productType", "collectorAreaM2", "installation", "pressureBar", "maxWaterTemperatureC", "dimensions", "weightKg", "compatibility"] }),
    category({ id: "flue-systems", slug: "flue-systems", parentId: "heating", level: 2, title: "Димоходи для котлів", shortTitle: "Димоходи", description: "Коаксіальні, роздільні та каскадні системи димовидалення.", order: 190, allowedFacetIds: accessoryFacets }),
    category({ id: "heating-components", slug: "heating-components", parentId: "heating", level: 2, title: "Комплектуючі для котлів і опалення", shortTitle: "Комплектуючі", description: "Монтажні, сервісні та фірмові компоненти котельного обладнання.", order: 200, allowedFacetIds: accessoryFacets }),
    category({ id: "boiler-doors-grates", slug: "doors-grates", parentId: "heating-components", level: 3, title: "Двері, рами та колосники", shortTitle: "Двері й колосники", description: "Двері, лутки, рами, колосники та зачепи для котлів.", order: 201, allowedFacetIds: accessoryFacets }),
    category({ id: "pellet-hoppers-augers", slug: "hoppers-augers", parentId: "heating-components", level: 3, title: "Бункери та шнекова подача", shortTitle: "Бункери й шнеки", description: "Пелетні бункери, шнеки та компоненти систем подачі палива.", order: 202, allowedFacetIds: accessoryFacets }),
    category({ id: "boiler-fans", slug: "boiler-fans", parentId: "heating-components", level: 3, title: "Вентилятори для котлів", shortTitle: "Вентилятори", description: "Наддувні та витяжні вентилятори котельного обладнання.", order: 203, allowedFacetIds: accessoryFacets }),
    category({ id: "boiler-cleaning-systems", slug: "cleaning-systems", parentId: "heating-components", level: 3, title: "Системи очищення котлів", shortTitle: "Очищення котлів", description: "Пневматичні та інші системи автоматичного очищення.", order: 204, allowedFacetIds: accessoryFacets }),
    category({ id: "boiler-hydraulic-nodes", slug: "boiler-hydraulic-nodes", parentId: "heating-components", level: 3, title: "Гідравлічні вузли котелень", shortTitle: "Гідровузли", description: "Гідравлічні модулі та вузли обв’язки котельного обладнання.", order: 205, allowedFacetIds: accessoryFacets }),
    category({ id: "heating-fittings", slug: "fittings-adapters", parentId: "heating-components", level: 3, title: "Муфти, переходи й адаптери", shortTitle: "Муфти й адаптери", description: "З’єднувальні елементи для колекторів, насосних груп і гідророздільників.", order: 206, allowedFacetIds: accessoryFacets }),
    category({ id: "branded-heating-accessories", slug: "brand-accessories", parentId: "heating-components", level: 3, title: "Інші фірмові комплектуючі", shortTitle: "Інші комплектуючі", description: "Інші підтверджені виробниками компоненти для монтажу, модернізації та сервісу.", order: 207, allowedFacetIds: accessoryFacets }),

    category({ id: "water-pumps", slug: "water-pumps", parentId: "water-supply", level: 2, title: "Насоси", shortTitle: "Насоси", description: "Насоси й комплектні установки за джерелом води та задачею.", order: 210 }),
    category({ id: "borehole-pumps", slug: "borehole-pumps", parentId: "water-pumps", level: 3, title: "Свердловинні насоси", shortTitle: "Свердловинні", description: "Занурювальні насоси й системи для свердловин та колодязів.", order: 211, allowedFacetIds: pumpFacets }),
    category({ id: "surface-pumps", slug: "surface-pumps", parentId: "water-pumps", level: 3, title: "Поверхневі насоси", shortTitle: "Поверхневі", description: "Поверхневі насоси для водопостачання, поливу й дощової води.", order: 212, allowedFacetIds: pumpFacets }),
    category({ id: "multistage-pumps", slug: "multistage-pumps", parentId: "water-pumps", level: 3, title: "Багатоступеневі насоси", shortTitle: "Багатоступеневі", description: "Горизонтальні багатоступеневі насоси для водопостачання та поливу.", order: 213, allowedFacetIds: pumpFacets }),
    category({ id: "pressure-boosting", slug: "pressure-boosting", parentId: "water-pumps", level: 3, title: "Підвищення тиску й насосні станції", shortTitle: "Підвищення тиску", description: "Насоси та установки для стабілізації тиску у водопостачанні.", order: 214, allowedFacetIds: pumpFacets }),
    category({ id: "drainage-pumps", slug: "drainage-pumps", parentId: "water-pumps", level: 3, title: "Дренажні насоси", shortTitle: "Дренажні", description: "Насоси для відведення дренажної та забрудненої води без фекалій.", order: 215, allowedFacetIds: pumpFacets }),
    category({ id: "sewage-pumps", slug: "sewage-pumps", parentId: "water-pumps", level: 3, title: "Каналізаційні та фекальні насоси", shortTitle: "Каналізаційні насоси", description: "Насоси для перекачування стічних і фекальних вод.", order: 216, allowedFacetIds: pumpFacets }),
    category({ id: "sewage-lifting-units", slug: "sewage-lifting-units", parentId: "water-pumps", level: 3, title: "Каналізаційні установки", shortTitle: "Каналізаційні установки", description: "Готові напірні установки для відведення стоків із санвузлів і будівель.", order: 217, allowedFacetIds: pumpFacets }),
    category({ id: "pool-pumps-filtration", slug: "pool-pumps-filtration", parentId: "water-pumps", level: 3, title: "Насоси та фільтрація для басейнів", shortTitle: "Для басейнів", description: "Насоси й фільтрувальні станції для приватних басейнів.", order: 218, allowedFacetIds: pumpFacets }),
    category({ id: "water-supply-components", slug: "components", parentId: "water-supply", level: 2, title: "Баки, автоматика й комплектуючі", shortTitle: "Комплектуючі", description: "Баки, контролери та монтажні компоненти насосного обладнання.", order: 220 }),
    category({ id: "pressure-tanks", slug: "pressure-tanks", parentId: "water-supply-components", level: 3, title: "Мембранні баки", shortTitle: "Мембранні баки", description: "Напірні мембранні баки для запасу води й стабілізації тиску.", order: 221, allowedFacetIds: ["productType", "volumeL", "pressureBar", "connection", "material", "installation", "temperature"] }),
    category({ id: "pump-automation", slug: "pump-automation", parentId: "water-supply-components", level: 3, title: "Автоматика насосів", shortTitle: "Автоматика насосів", description: "Контролери запуску, зупинки й захисту насосів.", order: 222, allowedFacetIds: automationFacets }),
    category({ id: "pump-accessories", slug: "pump-accessories", parentId: "water-supply-components", level: 3, title: "Монтажні комплектуючі для насосів", shortTitle: "Комплектуючі", description: "Клапани, колектори, кабелі, монтажні комплекти та інші компоненти.", order: 223, allowedFacetIds: accessoryFacets }),
    category({ id: "pump-services", slug: "pump-services", parentId: "water-supply-components", level: 3, title: "Пусконалагодження насосного обладнання", shortTitle: "Пусконалагодження", description: "Сервісна позиція, доступна через сервісний центр, а не товарну видачу.", order: 229, status: "internal", visibility: "service", allowedFacetIds: ["productType", "compatibility"] }),

    category({ id: "drinking-water-systems", slug: "drinking-water", parentId: "water-treatment", level: 2, title: "Питна вода", shortTitle: "Питна вода", description: "Системи під мийку для приготування питної води.", order: 310 }),
    category({ id: "reverse-osmosis", slug: "reverse-osmosis", parentId: "drinking-water-systems", level: 3, title: "Зворотний осмос", shortTitle: "Зворотний осмос", description: "Побутові та професійні системи зворотного осмосу.", order: 311, allowedFacetIds: treatmentFacets, legacyGroup: "drinking-water-systems", legacyGroupTitle: "Системи питної води" }),
    category({ id: "flow-filters", slug: "flow-filters", parentId: "drinking-water-systems", level: 3, title: "Проточні фільтри", shortTitle: "Проточні фільтри", description: "Проточні системи доочищення питної води.", order: 312, allowedFacetIds: treatmentFacets, legacyGroup: "drinking-water-systems", legacyGroupTitle: "Системи питної води" }),
    category({ id: "whole-house-treatment", slug: "whole-house", parentId: "water-treatment", level: 2, title: "Очищення води для будинку", shortTitle: "Для всього будинку", description: "Системи підготовки води на вході до будинку або комерційного об’єкта.", order: 320 }),
    category({ id: "complex-treatment", slug: "complex-treatment", parentId: "whole-house-treatment", level: 3, title: "Комплексне очищення", shortTitle: "Комплексне очищення", description: "Системи одночасного видалення кількох типів домішок.", order: 321, allowedFacetIds: treatmentFacets, legacyGroup: "whole-house-treatment", legacyGroupTitle: "Очищення води для будинку" }),
    category({ id: "water-softening", slug: "water-softening", parentId: "whole-house-treatment", level: 3, title: "Пом’якшення води", shortTitle: "Пом’якшення", description: "Системи зниження жорсткості води.", order: 322, allowedFacetIds: treatmentFacets, legacyGroup: "whole-house-treatment", legacyGroupTitle: "Очищення води для будинку" }),
    category({ id: "chlorine-odor-removal", slug: "chlorine-odor-removal", parentId: "whole-house-treatment", level: 3, title: "Видалення хлору та запаху", shortTitle: "Хлор і запах", description: "Системи видалення хлору, запахів і органічних домішок.", order: 323, allowedFacetIds: treatmentFacets, legacyGroup: "whole-house-treatment", legacyGroupTitle: "Очищення води для будинку" }),
    category({ id: "mechanical-treatment", slug: "mechanical-treatment", parentId: "whole-house-treatment", level: 3, title: "Механічне очищення", shortTitle: "Механічне очищення", description: "Системи попереднього очищення від піску, мулу та іржі.", order: 324, allowedFacetIds: treatmentFacets, legacyGroup: "whole-house-treatment", legacyGroupTitle: "Очищення води для будинку" }),
    category({ id: "mainline-filtration", slug: "mainline-filtration", parentId: "water-treatment", level: 2, title: "Магістральна фільтрація", shortTitle: "Магістральні фільтри", description: "Корпуси та картриджі для очищення води на вході.", order: 330 }),
    category({ id: "mainline-filters-housings", slug: "filter-housings", parentId: "mainline-filtration", level: 3, title: "Корпуси магістральних фільтрів", shortTitle: "Корпуси фільтрів", description: "Корпуси та готові магістральні фільтри.", order: 331, allowedFacetIds: treatmentFacets, legacyGroup: "whole-house-treatment", legacyGroupTitle: "Очищення води для будинку" }),
    category({ id: "mainline-cartridges", slug: "mainline-cartridges", parentId: "mainline-filtration", level: 3, title: "Картриджі магістральних фільтрів", shortTitle: "Магістральні картриджі", description: "Змінні картриджі для магістрального очищення води.", order: 332, allowedFacetIds: treatmentFacets, legacyGroup: "replacement-elements", legacyGroupTitle: "Змінні елементи" }),
    category({ id: "replacement-elements", slug: "replacement-elements", parentId: "water-treatment", level: 2, title: "Змінні елементи питних систем", shortTitle: "Змінні елементи", description: "Комплекти та окремі картриджі для питних систем.", order: 340 }),
    category({ id: "drinking-system-cartridges", slug: "drinking-system-cartridges", parentId: "replacement-elements", level: 3, title: "Картриджі питних систем", shortTitle: "Картриджі питних систем", description: "Змінні елементи для проточних фільтрів і систем зворотного осмосу.", order: 341, allowedFacetIds: treatmentFacets, legacyGroup: "replacement-elements", legacyGroupTitle: "Змінні елементи" }),
    category({ id: "materials-reagents", slug: "materials-reagents", parentId: "water-treatment", level: 2, title: "Завантаження та реагенти", shortTitle: "Матеріали", description: "Фільтрувальні завантаження й матеріали для систем очищення.", order: 350 }),
    category({ id: "filter-media", slug: "filter-media", parentId: "materials-reagents", level: 3, title: "Фільтрувальні матеріали", shortTitle: "Фільтрувальні матеріали", description: "Завантаження та матеріали для систем очищення води.", order: 351, allowedFacetIds: treatmentFacets, legacyGroup: "materials-reagents", legacyGroupTitle: "Матеріали та реагенти" }),
    category({ id: "water-disinfection", slug: "disinfection", parentId: "water-treatment", level: 2, title: "Знезараження води", shortTitle: "Знезараження", description: "Обладнання для безреагентного знезараження проточної води.", order: 360 }),
    category({ id: "uv-disinfection", slug: "uv-c-lamps", parentId: "water-disinfection", level: 3, title: "Проточні UV-C лампи", shortTitle: "UV-C лампи", description: "Проточні ультрафіолетові лампи для знезараження питної та побутової води.", order: 361, allowedFacetIds: ["productType", "flowM3h", "powerKw", "connection", "pressureBar", "voltage", "temperature"] }),

    category({ id: "smart-home-hubs", slug: "hubs-panels", parentId: "smart-home", level: 2, title: "Центральні блоки та панелі", shortTitle: "Блоки й панелі", description: "Головні контролери, панелі та інтерфейси системи Sinum.", order: 410, allowedFacetIds: automationFacets }),
    category({ id: "smart-lighting", slug: "lighting-switches", parentId: "smart-home", level: 2, title: "Освітлення, вимикачі й димери", shortTitle: "Освітлення", description: "Сенсорні вимикачі, димери й RGB-модулі керування освітленням.", order: 420, allowedFacetIds: automationFacets }),
    category({ id: "smart-shutters", slug: "shutters-blinds", parentId: "smart-home", level: 2, title: "Керування ролетами та жалюзі", shortTitle: "Ролети й жалюзі", description: "Вимикачі та модулі автоматизації ролет і жалюзі.", order: 430, allowedFacetIds: automationFacets }),
    category({ id: "smart-relays", slug: "relays-sockets", parentId: "smart-home", level: 2, title: "Реле, розетки та DIN-модулі", shortTitle: "Реле й розетки", description: "Силові реле, розетки, вхідні плати та модулі для щита.", order: 440, allowedFacetIds: automationFacets }),
    category({ id: "smart-sensors", slug: "sensors-security", parentId: "smart-home", level: 2, title: "Датчики, безпека та якість повітря", shortTitle: "Датчики й безпека", description: "Датчики диму, протікання, відкривання, руху та якості повітря.", order: 450, allowedFacetIds: automationFacets }),
    category({ id: "smart-climate", slug: "climate-control", parentId: "smart-home", level: 2, title: "Клімат і терморегуляція", shortTitle: "Клімат", description: "Терморегулятори, клапани та модулі кліматичного керування Sinum.", order: 460, allowedFacetIds: automationFacets }),
    category({ id: "smart-accessories", slug: "power-connectivity", parentId: "smart-home", level: 2, title: "Живлення, зв’язок і монтаж", shortTitle: "Живлення й монтаж", description: "Блоки живлення, кабелі, ретранслятори, рамки та монтажні аксесуари.", order: 470, allowedFacetIds: accessoryFacets }),

    category({ id: "air-conditioners", slug: "air-conditioners", parentId: "climate", level: 2, title: "Кондиціонери", shortTitle: "Кондиціонери", description: "Побутові спліт- і мультиспліт-системи для охолодження та обігріву.", order: 510, allowedFacetIds: ["productType", "coolingCapacityKw", "heatOutputKw", "refrigerant", "seer", "scop", "energyClass", "soundLevelDb", "voltage", "dimensions", "weightKg"] }),
    category({ id: "humidification", slug: "humidification", parentId: "climate", level: 2, title: "Туманоутворення та зволоження", shortTitle: "Туманоутворення", description: "Готові системи та комплектуючі для туманоутворення й охолодження повітря.", order: 520 }),
    category({ id: "misting-systems", slug: "systems", parentId: "humidification", level: 3, title: "Системи туманоутворення", shortTitle: "Готові системи", description: "Комплектні системи туманоутворення за кількістю форсунок.", order: 521, allowedFacetIds: pumpFacets }),
    category({ id: "misting-components", slug: "components", parentId: "humidification", level: 3, title: "Компоненти туманоутворення", shortTitle: "Компоненти", description: "Форсунки, трубки, з’єднання та монтажні компоненти систем.", order: 522, allowedFacetIds: accessoryFacets }),
    category({ id: "feed-grinders", slug: "feed-grinders", parentId: "household-equipment", level: 2, title: "Кормоподрібнювачі", shortTitle: "Кормоподрібнювачі", description: "Електричні подрібнювачі зернових культур і коренеплодів.", order: 610, allowedFacetIds: ["productType", "powerKw", "capacityKgh", "rotationRpm", "voltage", "protectionClass", "weightKg"] })
  ];

  const rawById = Object.fromEntries(definitions.map(item => [item.id, item]));
  const sectionIdFor = item => {
    let current = item;
    while (current?.parentId) current = rawById[current.parentId];
    return current?.id || "";
  };
  const nodes = Object.freeze(definitions.map(item => Object.freeze({
    ...item,
    name: item.title,
    metaTitle: item.seo?.title || `${item.title} | ТД «Софіївка»`,
    sectionId: sectionIdFor(item),
    facetIds: Object.freeze([...(item.allowedFacetIds || [])]),
    allowedFacetIds: Object.freeze([...(item.allowedFacetIds || [])]),
    seo: Object.freeze({ ...(item.seo || {}) })
  })));
  const byId = Object.freeze(Object.fromEntries(nodes.map(item => [item.id, item])));
  const childrenOf = parentId => nodes.filter(item => item.parentId === parentId).sort((a, b) => a.order - b.order);
  const descendantsOf = parentId => {
    const result = [];
    const visit = id => childrenOf(id).forEach(child => { result.push(child); visit(child.id); });
    visit(parentId);
    return result;
  };

  window.sofievkaTaxonomy = Object.freeze({
    nodes,
    byId,
    businessSections: Object.freeze(nodes.filter(item => item.level === 1).sort((a, b) => a.order - b.order)),
    productCategories: Object.freeze(nodes.filter(item => item.level > 1).sort((a, b) => a.order - b.order)),
    childrenOf,
    descendantsOf
  });
})();
/* Source: catalog/attribute-schema.js */
(function () {
  "use strict";

  const define = (id, definition) => Object.freeze({
    id,
    type: "select",
    unit: "",
    aliases: Object.freeze([]),
    options: null,
    filterable: true,
    sortable: false,
    categoryScope: Object.freeze([]),
    rank: 100,
    ...definition,
    aliases: Object.freeze([...(definition.aliases || [])]),
    categoryScope: Object.freeze([...(definition.categoryScope || [])])
  });

  const definitions = Object.freeze({
    productType: define("productType", { label: "Тип обладнання", rank: 1, aliases: [/^тип обладнання$/i, /^тип продукту$/i, /^категорія$/i] }),
    purpose: define("purpose", { label: "Призначення", rank: 2, aliases: [/^призначення$/i, /^застосування$/i] }),
    type: define("type", { label: "Тип", rank: 3, aliases: [/^тип$/i, /^виконання$/i] }),
    technology: define("technology", { label: "Технологія очищення", rank: 4, aliases: [/^технологія(?: очищення)?$/i, /^технология$/i] }),
    waterSource: define("waterSource", { label: "Джерело води", rank: 5, aliases: [/^джерело води$/i] }),
    installation: define("installation", { label: "Монтаж", rank: 6, aliases: [/^монтаж$/i, /^встановлення$/i, /^спосіб встановлення$/i] }),
    capacityLh: define("capacityLh", { label: "Продуктивність, л/год", type: "number", unit: "л/год", rank: 7, aliases: [/^продуктивність(?:,?\s*л\/год)?$/i] }),
    flowM3h: define("flowM3h", { label: "Продуктивність, м³/год", type: "number", unit: "м³/год", rank: 8, aliases: [/^qmax$/i, /^gmax$/i, /^макс\.?\s*продуктивність$/i, /^максимальна витрата$/i, /^витрата$/i, /^пропускна здатність(?:\s*\(м[³3]\/год\))?$/i, /^швидкість потоку(?:\s*\(м[³3]\/год\))?$/i, /^номінальна витрата$/i] }),
    capacityKgh: define("capacityKgh", { label: "Продуктивність, кг/год", type: "number", unit: "кг/год", rank: 8.1, aliases: [/^продуктивність(?:,?\s*кг\/год)?$/i, /^продуктивність.*кг\/год$/i] }),
    rotationRpm: define("rotationRpm", { label: "Частота обертання, об/хв", type: "number", unit: "об/хв", rank: 8.2, aliases: [/^частота обертання$/i, /^оберти двигуна$/i] }),
    kvs: define("kvs", { label: "Kvs, м³/год", type: "number", unit: "м³/год", rank: 9, aliases: [/^kvs(?:\s*\(.+\))?$/i, /^kv$/i] }),
    headM: define("headM", { semanticId: "maxHeadM", label: "Максимальний напір, м", type: "number", unit: "м", rank: 9, aliases: [/^hmax$/i, /^макс\.?\s*напір$/i, /^максимальний напір$/i, /^висота підйому$/i] }),
    powerKw: define("powerKw", { label: "Потужність, кВт", type: "number", unit: "кВт", rank: 10, aliases: [/^потужність$/i, /^споживана потужність$/i, /^макс\.?\s*споживана потужність$/i, /^мощность$/i] }),
    heatOutputKw: define("heatOutputKw", { label: "Теплова потужність, кВт", type: "number", unit: "кВт", rank: 11, aliases: [/^теплова потужність$/i, /^номінальна теплова потужність$/i, /^максимальна номінальна теплопродуктивність/i, /^макс\. номінальне теплове навантаження/i, /^потужність опалення$/i, /^потужність макс\./i, /^qmax:.*(?:20|10)/i] }),
    coolingCapacityKw: define("coolingCapacityKw", { label: "Потужність охолодження, кВт", type: "number", unit: "кВт", rank: 11.05, aliases: [/^потужність охолодження$/i, /^холодопродуктивність$/i, /^номінальна потужність.*охолодження/i] }),
    dhwFlowLMin: define("dhwFlowLMin", { label: "Продуктивність ГВП, л/хв", type: "number", unit: "л/хв", rank: 11.06, aliases: [/^продуктивність гвп/i, /^витрата гарячої води/i, /^кількість гарячої води/i] }),
    energyClass: define("energyClass", { label: "Клас енергоефективності", rank: 11.07, aliases: [/^клас енергоспоживання$/i, /^клас енергоефективності$/i, /^енергетичний клас$/i] }),
    refrigerant: define("refrigerant", { label: "Холодоагент", rank: 11.08, aliases: [/^холодоагент$/i, /^тип холодоагенту$/i] }),
    cop: define("cop", { label: "COP", type: "number", rank: 11.09, aliases: [/^cop(?:\s|$)/i, /^коефіцієнт перетворення cop/i] }),
    scop: define("scop", { label: "SCOP", type: "number", rank: 11.10, aliases: [/^scop(?:\s|$)/i, /^сезонний коефіцієнт.*scop/i] }),
    seer: define("seer", { label: "SEER", type: "number", rank: 11.11, aliases: [/^seer(?:\s|$)/i, /^сезонний коефіцієнт.*seer/i] }),
    soundLevelDb: define("soundLevelDb", { label: "Рівень шуму, дБ", type: "number", unit: "дБ", rank: 11.12, aliases: [/^рівень (?:звуку|шуму)/i, /^звукова потужність/i, /^рівень звукової потужності/i] }),
    collectorAreaM2: define("collectorAreaM2", { label: "Площа колектора, м²", type: "number", unit: "м²", rank: 11.13, aliases: [/^площа колектора$/i, /^загальна площа$/i, /^площа абсорбера$/i] }),
    fuel: define("fuel", { label: "Паливо", rank: 11.1, aliases: [/^паливо$/i, /^вид палива$/i, /^основне паливо$/i] }),
    efficiencyPercent: define("efficiencyPercent", { label: "ККД, %", type: "number", unit: "%", rank: 11.2, aliases: [/^ккд/i, /^коефіцієнт корисної дії/i] }),
    waterVolumeL: define("waterVolumeL", { label: "Водяна ємність, л", type: "number", unit: "л", rank: 11.3, aliases: [/^водяна ємність котла/i, /^об[’'`]?єм води/i] }),
    fireboxVolumeL: define("fireboxVolumeL", { label: "Об’єм топки, л", type: "number", unit: "л", rank: 11.4, aliases: [/^об[’'`]?єм топки/i, /^топка:\s*об[’'`]?єм/i] }),
    chimneyDiameterMm: define("chimneyDiameterMm", { label: "Діаметр димоходу, мм", type: "number", unit: "мм", rank: 11.5, aliases: [/^діаметр димоходу/i, /^приєднувальні розміри димоходу/i, /^рекомендовані параметри димоходу:\s*внутрішній діаметр/i] }),
    chimneyHeightM: define("chimneyHeightM", { label: "Мінімальна висота димоходу, м", type: "number", unit: "м", rank: 11.6, aliases: [/^мінімальна висота димоходу/i, /^рекомендовані параметри димоходу:\s*висота/i] }),
    consumptionPowerW: define("consumptionPowerW", { label: "Споживання електроенергії, Вт", type: "number", unit: "Вт", rank: 11.7, aliases: [/^споживання електроенергії/i, /^електрична потужність/i] }),
    maxWaterTemperatureC: define("maxWaterTemperatureC", { label: "Максимальна температура води, °C", type: "number", unit: "°C", rank: 11.8, aliases: [/^максимальна температура води/i, /^макс\.? температура теплоносія/i] }),
    mountingLengthMm: define("mountingLengthMm", { semanticId: "installationLengthMm", label: "Монтажна довжина, мм", type: "number", unit: "мм", rank: 12, aliases: [/^довжина насоса$/i, /^монтажна довжина$/i, /^будівельна довжина$/i] }),
    diameterMm: define("diameterMm", { label: "Діаметр, мм", type: "number", unit: "мм", rank: 13, aliases: [/^d\s*\(?мм\)?$/i, /^d\s*\(?mm\)?$/i, /^діаметр,?\s*мм$/i, /^диаметр,?\s*мм$/i] }),
    widthMm: define("widthMm", { label: "Ширина, мм", type: "number", unit: "мм", rank: 14, filterable: false, aliases: [/^ширина(?:\s*\(мм\))?$/i, /^w\s*\(?mm\)?$/i] }),
    heightMm: define("heightMm", { label: "Висота, мм", type: "number", unit: "мм", rank: 15, filterable: false, aliases: [/^висота(?:\s*\(мм\))?$/i, /^h\s*\(?mm\)?$/i, /^h\s*\(?мм\)?$/i] }),
    depthMm: define("depthMm", { label: "Глибина, мм", type: "number", unit: "мм", rank: 16, filterable: false, aliases: [/^глибина(?:\s*\(мм\))?$/i, /^глубина(?:\s*\(мм\))?$/i] }),
    dimensions: define("dimensions", { label: "Габарити", rank: 16.5, filterable: false, aliases: [/^розмір(?:и)?$/i, /^габарит(?:и|ні розміри)?$/i] }),
    volumeL: define("volumeL", { label: "Об’єм, л", type: "number", unit: "л", rank: 17, aliases: [/^об[’'`]?єм(?: бойлера| бака)?$/i, /^объем$/i, /^місткість$/i] }),
    weightKg: define("weightKg", { label: "Вага, кг", type: "number", unit: "кг", rank: 18, filterable: false, aliases: [/^вага(?:\s*\(кг\))?$/i, /^маса(?:\s*\(кг\))?$/i, /^weight$/i] }),
    filtrationMicron: define("filtrationMicron", { label: "Тонкість фільтрації, мкм", type: "number", unit: "мкм", rank: 19, aliases: [/^тонкість фільтрації$/i] }),
    pressureBar: define("pressureBar", { label: "Робочий тиск, бар", type: "number", unit: "бар", rank: 15, aliases: [/^робочий тиск$/i, /^макс\.?\s*тиск$/i, /^максимальний робочий тиск$/i, /^тиск максимальний$/i, /^давление$/i] }),
    diameter: define("diameter", { label: "Діаметр / DN", rank: 16, aliases: [/^dn$/i, /^діаметр$/i, /^диаметр$/i] }),
    connection: define("connection", { label: "Підключення", rank: 20, aliases: [/^підключення$/i, /^приєднання$/i, /^підключення котла$/i, /^підключення системи$/i, /^підключення контура? опалення$/i, /^підключення до контуру опалення$/i, /^підключення до колектора$/i, /^підключення контурів$/i, /^різьба(?: підключення)?$/i, /^з'єднання$/i] }),
    compatibility: define("compatibility", { label: "Сумісність", rank: 21, filterable: false, aliases: [/^сумісність$/i] }),
    protectionClass: define("protectionClass", { label: "Клас захисту", rank: 22, aliases: [/^клас захисту$/i, /^ступінь захисту$/i, /^ip$/i] }),
    outlets: define("outlets", { label: "Кількість виходів", rank: 18, aliases: [/^кількість виходів$/i, /^виходи$/i, /^кількість контурів$/i] }),
    material: define("material", { label: "Матеріал", rank: 19, aliases: [/^матеріал корпусу$/i, /^матеріал$/i, /^материал$/i] }),
    voltage: define("voltage", { label: "Живлення", rank: 20, aliases: [/^напруга$/i, /^живлення$/i, /^привід\s*\/\s*напруга$/i, /^напряжение$/i] }),
    control: define("control", { label: "Тип керування", rank: 21, aliases: [/^керування$/i, /^управління$/i, /^управление$/i, /^тип керування$/i] }),
    communication: define("communication", { label: "Спосіб зв’язку", rank: 21.1, aliases: [/^комунікація$/i, /^спосіб зв[’'`]язку$/i, /^тип зв[’'`]язку$/i, /^зв[’'`]язок$/i] }),
    zones: define("zones", { label: "Кількість зон", type: "number", rank: 21.2, aliases: [/^кількість зон$/i, /^зони$/i, /^кількість приміщень$/i] }),
    temperature: define("temperature", { label: "Температурний діапазон", rank: 22, aliases: [/^діапазон температур$/i, /^температура рідини$/i, /^макс\.?\s*температура$/i, /^максимальна температура$/i, /^температура$/i] }),
    eei: define("eei", { label: "Індекс енергоефективності EEI", rank: 22.1, aliases: [/^індекс енергетичної ефективності(?: \(eei\))?$/i, /^eei$/i] }),
    selfPriming: define("selfPriming", { label: "Самовсмоктування", rank: 22.2, aliases: [/^самовсмоктувальне виконання$/i] }),
    maxImmersionDepthM: define("maxImmersionDepthM", { label: "Максимальна глибина занурення, м", type: "number", unit: "м", rank: 22.3, aliases: [/^максимальна глибина занурення$/i] }),
    freePassageMm: define("freePassageMm", { label: "Вільний прохід, мм", type: "number", unit: "мм", rank: 22.4, aliases: [/^вільний сферичний прохід$/i, /^вільний прохід$/i] }),
    cableLengthM: define("cableLengthM", { label: "Довжина кабелю, м", type: "number", unit: "м", rank: 22.5, aliases: [/^довжина кабелю$/i] }),
    floatSwitch: define("floatSwitch", { label: "Поплавковий вимикач", rank: 22.6, aliases: [/^поплавковий вимикач$/i] }),
    waterType: define("waterType", { label: "Тип води", rank: 23, aliases: [/^вода$/i, /^тип води$/i] }),
    format: define("format", { label: "Формат / типорозмір", rank: 24, aliases: [/^формат$/i, /^типорозмір$/i] }),
    pump: define("pump", { label: "Помпа", rank: 25, aliases: [/^помпа$/i, /^насос підвищення тиску$/i] }),
    mineralizer: define("mineralizer", { label: "Мінералізація", rank: 26, aliases: [/^мінералізація$/i, /^мінералізатор$/i] }),
    flowType: define("flowType", { label: "Тип системи", rank: 27, aliases: [/^тип системи$/i, /^формат системи$/i] }),
    scope: define("scope", { label: "Для об’єкта", rank: 28, aliases: [/^для об'єкта$/i, /^сфера застосування$/i] })
  });

  const valueLabels = Object.freeze({
    yes: "Так",
    no: "Ні",
    direct: "Прямоточна",
    tank: "З баком",
    apartment: "Квартира",
    house: "Будинок",
    business: "Бізнес / HoReCa",
    mains: "Водогін",
    borehole: "Свердловина",
    "under-sink": "Під мийку",
    mechanical: "Механічне очищення",
    softening: "Пом’якшення",
    "iron-removal": "Знезалізнення",
    "chlorine-odor": "Хлор і запахи",
    complex: "Комплексне очищення"
  });

  const cardPriorityByCategory = Object.freeze({
    "circulation-pumps": Object.freeze(["headM", "flowM3h", "mountingLengthMm", "connection", "diameter", "powerKw"]),
    "underfloor-heating": Object.freeze(["diameter", "connection", "material", "pressureBar", "temperature", "outlets"]),
    "distribution-hydraulics": Object.freeze(["flowM3h", "heatOutputKw", "outlets", "connection", "diameter", "pressureBar", "mountingLengthMm"]),
    automation: Object.freeze(["productType", "control", "communication", "zones", "installation", "voltage"]),
    valves: Object.freeze(["diameter", "connection", "kvs", "pressureBar", "control", "material", "temperature"]),
    "heating-components": Object.freeze(["productType", "compatibility", "communication", "installation", "voltage", "protectionClass"]),
    "reverse-osmosis": Object.freeze(["capacityLh", "pump", "mineralizer", "flowType", "installation", "technology"]),
    "drinking-system-cartridges": Object.freeze(["purpose", "type", "filtrationMicron", "compatibility", "mineralizer"]),
    "mainline-cartridges": Object.freeze(["purpose", "type", "filtrationMicron", "format", "compatibility"]),
    "mainline-filters-housings": Object.freeze(["connection", "waterType", "purpose", "scope"]),
    "filter-media": Object.freeze(["purpose", "type", "waterSource"]),
    "complex-treatment": Object.freeze(["purpose", "flowM3h", "waterSource", "installation", "scope", "technology"]),
    "water-softening": Object.freeze(["purpose", "flowM3h", "waterSource", "installation", "scope"]),
    "mechanical-treatment": Object.freeze(["purpose", "flowM3h", "waterSource", "installation", "scope"]),
    "chlorine-odor-removal": Object.freeze(["purpose", "flowM3h", "waterSource", "installation", "scope"]),
    "flow-filters": Object.freeze(["type", "purpose", "installation", "filtrationMicron"]),
    "multistage-pumps": Object.freeze(["powerKw", "selfPriming", "connection", "pressureBar", "voltage", "protectionClass"]),
    "drainage-pumps": Object.freeze(["powerKw", "freePassageMm", "maxImmersionDepthM", "floatSwitch", "connection", "protectionClass"]),
    "borehole-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "connection", "voltage", "protectionClass"]),
    "surface-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "selfPriming", "connection", "pressureBar"]),
    "pressure-boosting": Object.freeze(["headM", "flowM3h", "powerKw", "pressureBar", "connection", "control"]),
    "sewage-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "freePassageMm", "connection", "protectionClass"]),
    "sewage-lifting-units": Object.freeze(["headM", "flowM3h", "powerKw", "volumeL", "connection", "control"]),
    "pressure-tanks": Object.freeze(["volumeL", "pressureBar", "connection", "material", "installation", "temperature"]),
    "pump-automation": Object.freeze(["pressureBar", "connection", "voltage", "protectionClass", "control", "temperature"]),
    "pump-accessories": Object.freeze(["compatibility", "connection", "material", "pressureBar", "weightKg"]),
    "pump-services": Object.freeze(["productType", "compatibility"]),
    "pool-pumps-filtration": Object.freeze(["flowM3h", "headM", "powerKw", "connection", "voltage", "protectionClass", "filtrationMicron"]),
    "feed-grinders": Object.freeze(["powerKw", "capacityKgh", "rotationRpm", "voltage", "protectionClass", "weightKg"]),
    "solid-fuel-boilers": Object.freeze(["heatOutputKw", "fuel", "efficiencyPercent", "fireboxVolumeL", "waterVolumeL", "chimneyDiameterMm"]),
    "gas-boilers": Object.freeze(["heatOutputKw", "type", "dhwFlowLMin", "efficiencyPercent", "installation", "energyClass"]),
    "heat-pumps": Object.freeze(["heatOutputKw", "coolingCapacityKw", "refrigerant", "cop", "scop", "energyClass"]),
    "hot-water-tanks": Object.freeze(["volumeL", "heatOutputKw", "pressureBar", "maxWaterTemperatureC", "connection", "material"]),
    "solar-thermal": Object.freeze(["collectorAreaM2", "installation", "pressureBar", "maxWaterTemperatureC", "dimensions", "weightKg"]),
    "flue-systems": Object.freeze(["compatibility", "diameter", "connection", "material", "installation"]),
    "air-conditioners": Object.freeze(["coolingCapacityKw", "heatOutputKw", "refrigerant", "seer", "scop", "soundLevelDb"]),
    "pellet-boilers": Object.freeze(["heatOutputKw", "fuel", "efficiencyPercent", "waterVolumeL", "chimneyDiameterMm", "consumptionPowerW"]),
    "heat-accumulators": Object.freeze(["volumeL", "pressureBar", "connection", "heightMm", "widthMm", "weightKg"]),
    "pellet-burners": Object.freeze(["heatOutputKw", "fuel", "consumptionPowerW", "voltage", "compatibility", "weightKg"]),
    "boiler-accessories": Object.freeze(["productType", "compatibility", "volumeL", "heatOutputKw", "connection", "weightKg"]),
    "industrial-heating": Object.freeze(["heatOutputKw", "capacityKgh", "fuel", "efficiencyPercent", "consumptionPowerW", "pressureBar"]),
    humidification: Object.freeze(["productType", "purpose", "flowM3h", "pressureBar", "voltage", "powerKw"])
  });

  const cardFallbackPriority = Object.freeze([
    "purpose", "type", "technology", "capacityLh", "flowM3h", "headM", "kvs", "powerKw", "heatOutputKw",
    "connection", "diameter", "diameterMm", "mountingLengthMm", "pressureBar", "filtrationMicron", "material",
    "control", "communication", "zones", "voltage", "protectionClass", "compatibility", "waterSource", "installation", "format", "pump",
    "mineralizer", "flowType", "scope", "volumeL", "weightKg", "capacityKgh", "rotationRpm", "widthMm", "heightMm", "depthMm", "dimensions", "eei", "selfPriming", "maxImmersionDepthM", "freePassageMm", "cableLengthM", "floatSwitch", "coolingCapacityKw", "dhwFlowLMin", "energyClass", "refrigerant", "cop", "scop", "seer", "soundLevelDb", "collectorAreaM2", "productType"
  ]);

  const pdpPriorityByCategory = Object.freeze({
    "circulation-pumps": Object.freeze(["headM", "flowM3h", "mountingLengthMm", "connection", "powerKw", "control"]),
    "underfloor-heating": Object.freeze(["diameter", "connection", "outlets", "material", "pressureBar", "temperature"]),
    "distribution-hydraulics": Object.freeze(["flowM3h", "heatOutputKw", "outlets", "connection", "diameter", "pressureBar"]),
    automation: Object.freeze(["productType", "control", "communication", "zones", "installation", "voltage", "protectionClass", "temperature"]),
    valves: Object.freeze(["diameter", "kvs", "pressureBar", "connection", "material", "control"]),
    "heating-components": Object.freeze(["productType", "compatibility", "communication", "installation", "voltage", "protectionClass"]),
    "reverse-osmosis": Object.freeze(["capacityLh", "pump", "mineralizer", "flowType", "installation", "technology"]),
    "drinking-system-cartridges": Object.freeze(["type", "purpose", "compatibility", "filtrationMicron", "mineralizer"]),
    "mainline-cartridges": Object.freeze(["filtrationMicron", "format", "type", "purpose", "compatibility"]),
    "mainline-filters-housings": Object.freeze(["connection", "format", "waterType", "purpose", "scope"]),
    "filter-media": Object.freeze(["type", "purpose", "waterSource", "format"]),
    "complex-treatment": Object.freeze(["flowM3h", "purpose", "waterSource", "installation", "scope", "technology"]),
    "water-softening": Object.freeze(["flowM3h", "purpose", "waterSource", "installation", "scope"]),
    "mechanical-treatment": Object.freeze(["flowM3h", "purpose", "waterSource", "installation", "scope"]),
    "chlorine-odor-removal": Object.freeze(["flowM3h", "purpose", "waterSource", "installation", "scope"]),
    "flow-filters": Object.freeze(["type", "purpose", "installation", "filtrationMicron", "flowM3h"]),
    "multistage-pumps": Object.freeze(["powerKw", "pressureBar", "selfPriming", "connection", "voltage", "protectionClass"]),
    "drainage-pumps": Object.freeze(["powerKw", "freePassageMm", "maxImmersionDepthM", "cableLengthM", "floatSwitch", "connection"]),
    "borehole-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "connection", "voltage", "protectionClass"]),
    "surface-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "selfPriming", "connection", "pressureBar"]),
    "pressure-boosting": Object.freeze(["headM", "flowM3h", "powerKw", "pressureBar", "connection", "control"]),
    "sewage-pumps": Object.freeze(["headM", "flowM3h", "powerKw", "freePassageMm", "maxImmersionDepthM", "connection"]),
    "sewage-lifting-units": Object.freeze(["headM", "flowM3h", "powerKw", "volumeL", "connection", "control"]),
    "pressure-tanks": Object.freeze(["volumeL", "pressureBar", "connection", "material", "installation", "temperature"]),
    "pump-automation": Object.freeze(["pressureBar", "connection", "voltage", "protectionClass", "control", "temperature"]),
    "pump-accessories": Object.freeze(["compatibility", "connection", "material", "pressureBar", "weightKg"]),
    "pump-services": Object.freeze(["productType", "compatibility"]),
    "pool-pumps-filtration": Object.freeze(["flowM3h", "headM", "powerKw", "connection", "voltage", "protectionClass", "filtrationMicron"]),
    "feed-grinders": Object.freeze(["powerKw", "capacityKgh", "rotationRpm", "voltage", "protectionClass", "weightKg"]),
    "solid-fuel-boilers": Object.freeze(["heatOutputKw", "fuel", "efficiencyPercent", "fireboxVolumeL", "waterVolumeL", "pressureBar", "maxWaterTemperatureC", "chimneyDiameterMm", "chimneyHeightM", "weightKg"]),
    "gas-boilers": Object.freeze(["heatOutputKw", "type", "dhwFlowLMin", "efficiencyPercent", "installation", "energyClass", "dimensions", "weightKg", "voltage", "protectionClass"]),
    "heat-pumps": Object.freeze(["heatOutputKw", "coolingCapacityKw", "refrigerant", "cop", "scop", "energyClass", "voltage", "soundLevelDb", "maxWaterTemperatureC", "dimensions", "weightKg"]),
    "hot-water-tanks": Object.freeze(["volumeL", "heatOutputKw", "pressureBar", "maxWaterTemperatureC", "connection", "material", "installation", "dimensions", "weightKg", "compatibility"]),
    "solar-thermal": Object.freeze(["collectorAreaM2", "installation", "pressureBar", "maxWaterTemperatureC", "dimensions", "weightKg", "compatibility"]),
    "flue-systems": Object.freeze(["compatibility", "diameter", "connection", "material", "installation", "dimensions"]),
    "air-conditioners": Object.freeze(["coolingCapacityKw", "heatOutputKw", "refrigerant", "seer", "scop", "energyClass", "soundLevelDb", "voltage", "dimensions", "weightKg"]),
    "pellet-boilers": Object.freeze(["heatOutputKw", "fuel", "efficiencyPercent", "waterVolumeL", "pressureBar", "maxWaterTemperatureC", "chimneyDiameterMm", "consumptionPowerW", "weightKg"]),
    "heat-accumulators": Object.freeze(["volumeL", "pressureBar", "connection", "material", "temperature", "heightMm", "widthMm", "weightKg"]),
    "pellet-burners": Object.freeze(["heatOutputKw", "fuel", "consumptionPowerW", "voltage", "compatibility", "weightKg"]),
    "boiler-accessories": Object.freeze(["productType", "compatibility", "volumeL", "heatOutputKw", "connection", "material", "consumptionPowerW", "weightKg"]),
    "industrial-heating": Object.freeze(["heatOutputKw", "capacityKgh", "fuel", "efficiencyPercent", "consumptionPowerW", "pressureBar", "weightKg"]),
    humidification: Object.freeze(["productType", "purpose", "flowM3h", "pressureBar", "voltage", "powerKw", "connection"])
  });

  const pdpFallbackPriority = Object.freeze([
    ...cardFallbackPriority,
    "outlets", "widthMm", "heightMm", "depthMm", "dimensions", "volumeL", "weightKg"
  ]);

  const cardAttributeLabels = Object.freeze({
    capacityLh: "Продуктивність",
    capacityKgh: "Продуктивність",
    rotationRpm: "Оберти",
    flowM3h: "Подача",
    headM: "Макс. напір",
    powerKw: "Потужність",
    heatOutputKw: "Теплова потужність",
    coolingCapacityKw: "Охолодження",
    dhwFlowLMin: "Продуктивність ГВП",
    energyClass: "Клас енергоефективності",
    refrigerant: "Холодоагент",
    cop: "COP",
    scop: "SCOP",
    seer: "SEER",
    soundLevelDb: "Рівень шуму",
    collectorAreaM2: "Площа колектора",
    fuel: "Паливо",
    efficiencyPercent: "ККД",
    waterVolumeL: "Водяна ємність",
    fireboxVolumeL: "Об’єм топки",
    chimneyDiameterMm: "Діаметр димоходу",
    chimneyHeightM: "Висота димоходу",
    consumptionPowerW: "Споживання",
    maxWaterTemperatureC: "Макс. температура води",
    mountingLengthMm: "Монтажна довжина",
    filtrationMicron: "Фільтрація",
    pressureBar: "Робочий тиск",
    diameter: "Діаметр / DN",
    diameterMm: "Діаметр",
    connection: "Підключення",
    protectionClass: "Клас захисту",
    outlets: "Кількість виходів",
    voltage: "Живлення",
    control: "Керування",
    communication: "Зв’язок",
    zones: "Кількість зон",
    temperature: "Температура",
    eei: "EEI",
    selfPriming: "Самовсмоктування",
    maxImmersionDepthM: "Глибина занурення",
    freePassageMm: "Вільний прохід",
    cableLengthM: "Довжина кабелю",
    floatSwitch: "Поплавок",
    waterSource: "Джерело води",
    productType: "Тип обладнання"
  });

  const seriesLabels = Object.freeze({
    "termojet-mega": "Mega",
    "termojet-box": "Box",
    "wilo-yonos-pico1-0": "Yonos PICO1.0",
    "wilo-star-z-nova": "Star-Z NOVA",
    "wilo-himulti-3": "HiMulti 3",
    "wilo-drain-tm-32": "Drain TM/TMW/TMR 32",
    "wilo-stratos-maxo": "Stratos MAXO",
    ...(window.sofievkaWiloSeriesLabels || {}),
    ...(window.sofievkaGrundfosSeriesLabels || {}),
    ...(window.sofievkaTekkhausSeriesLabels || {}),
    ...(window.sofievkaTechSeriesLabels || {}),
    ...(window.sofievkaHeatingBrandsSeriesLabels || {}),
    ...(window.sofievkaBaxiBuderusSeriesLabels || {})
  });

  window.sofievkaAttributeSchema = Object.freeze({ definitions, valueLabels, cardPriorityByCategory, cardFallbackPriority, pdpPriorityByCategory, pdpFallbackPriority, cardAttributeLabels, seriesLabels });
})();
/* Source: catalog/source-mappings.js */
(function () {
  "use strict";

  const freezeMapping = mapping => Object.freeze({
    categoryId: "",
    classifier: "",
    seriesId: null,
    collectionIds: Object.freeze([]),
    tags: Object.freeze([]),
    mappingStatus: "mapped",
    ...mapping,
    collectionIds: Object.freeze([...(mapping.collectionIds || [])]),
    tags: Object.freeze([...(mapping.tags || [])])
  });

  const suppliers = Object.freeze({
    ecosoft: Object.freeze({ id: "ecosoft", brandId: "ecosoft", sourceName: "Каталог товарів" }),
    termojet: Object.freeze({ id: "termojet", brandId: "termojet", sourceName: "Termojet XML catalog", imageOrigin: "https://termojet.com.ua" }),
    wilo: Object.freeze({ id: "wilo", brandId: "wilo", sourceName: "Офіційний каталог Wilo Україна" }),
    grundfos: Object.freeze({ id: "grundfos", brandId: "grundfos", sourceName: "Офіційний каталог Grundfos Україна" }),
    tekkhaus: Object.freeze({ id: "tekkhaus", brandId: "tekk", sourceName: "Офіційний магазин TEKK HAUS" }),
    tech: Object.freeze({ id: "tech", brandId: "tech", sourceName: "Офіційний каталог TECH Controllers Україна" }),
    altep: Object.freeze({ id: "altep", brandId: "altep", sourceName: "Офіційний каталог Altep" }),
    feniks: Object.freeze({ id: "feniks", brandId: "feniks", sourceName: "Офіційний каталог FENIKS" }),
    focus: Object.freeze({ id: "focus", brandId: "focus", sourceName: "Офіційний каталог FOCUS / FireBox" }),
    baxi: Object.freeze({ id: "baxi", brandId: "baxi", sourceName: "Офіційний каталог BAXI Україна" }),
    buderus: Object.freeze({ id: "buderus", brandId: "buderus", sourceName: "Офіційний каталог Buderus Україна" })
  });

  const categoryMappings = Object.freeze({
    ecosoft: Object.freeze({
      "reverse-osmosis": freezeMapping({ categoryId: "reverse-osmosis" }),
      "flow-filters": freezeMapping({ categoryId: "flow-filters" }),
      "mainline-filters": freezeMapping({ categoryId: "mainline-filters-housings" }),
      "water-filter-cartridges": freezeMapping({ categoryId: "drinking-system-cartridges" }),
      "mainline-cartridges": freezeMapping({ categoryId: "mainline-cartridges" }),
      "filter-media": freezeMapping({ categoryId: "filter-media" }),
      horeca: freezeMapping({ categoryId: "reverse-osmosis", collectionIds: ["horeca"], tags: ["horeca"] }),
      "filtration-systems": freezeMapping({ categoryId: "", mappingStatus: "classifier", tags: ["whole-house-treatment"] })
    }),
    termojet: Object.freeze({
      "termojet-nasosy": freezeMapping({ classifier: "termojet-pumps", mappingStatus: "classifier" }),
      "termojet-kolektory-pidloha": freezeMapping({ categoryId: "underfloor-heating" }),
      "termojet-rozpodilchi-kolektory": freezeMapping({ categoryId: "distribution-manifolds" }),
      "termojet-separatory": freezeMapping({ categoryId: "air-dirt-separators" }),
      "termojet-nasosni-hrupy": freezeMapping({ categoryId: "pump-groups" }),
      "termojet-kolektory-z-hidrostrilkoyu": freezeMapping({ categoryId: "manifolds-with-hydraulic-separator" }),
      "termojet-hidravlichni-rozdilnyky": freezeMapping({ categoryId: "hydraulic-separators" }),
      "termojet-zonalne-keruvannya": freezeMapping({ categoryId: "zone-control" }),
      "termojet-avtomatyka": freezeMapping({ classifier: "heating-automation", mappingStatus: "classifier" }),
      "termojet-klapany": freezeMapping({ categoryId: "heating-valves" }),
      "termojet-balansuval-klapany": freezeMapping({ categoryId: "heating-valves" }),
      "termojet-termojet-mega": freezeMapping({ classifier: "heating-component", seriesId: "termojet-mega", mappingStatus: "classifier" }),
      "termojet-termojet-box": freezeMapping({ classifier: "heating-component", seriesId: "termojet-box", mappingStatus: "classifier" }),
      "termojet-rozprodazh": freezeMapping({ classifier: "heating-component", collectionIds: ["sale"], tags: ["sale"], mappingStatus: "review" }),
      "termojet-dodatkove": freezeMapping({ classifier: "heating-component", tags: ["additional"], mappingStatus: "review" })
    }),
    wilo: Object.freeze({
      "wilo-circulation": freezeMapping({ categoryId: "system-circulation-pumps" }),
      "wilo-dhw-circulation": freezeMapping({ categoryId: "dhw-circulation-pumps" }),
      "wilo-system-circulation": freezeMapping({ categoryId: "system-circulation-pumps" }),
      "wilo-multistage": freezeMapping({ categoryId: "multistage-pumps" }),
      "wilo-drainage": freezeMapping({ categoryId: "drainage-pumps" }),
      "wilo-borehole": freezeMapping({ categoryId: "borehole-pumps" }),
      "wilo-surface": freezeMapping({ categoryId: "surface-pumps" }),
      "wilo-pressure": freezeMapping({ categoryId: "pressure-boosting" }),
      "wilo-sewage": freezeMapping({ categoryId: "sewage-pumps" }),
      "wilo-lifting": freezeMapping({ categoryId: "sewage-lifting-units" })
    }),
    grundfos: Object.freeze({
      "grundfos-circulation": freezeMapping({ categoryId: "system-circulation-pumps" }),
      "grundfos-dhw-circulation": freezeMapping({ categoryId: "dhw-circulation-pumps" }),
      "grundfos-surface": freezeMapping({ categoryId: "surface-pumps" }),
      "grundfos-pressure": freezeMapping({ categoryId: "pressure-boosting" }),
      "grundfos-drainage": freezeMapping({ categoryId: "drainage-pumps" }),
      "grundfos-sewage": freezeMapping({ categoryId: "sewage-pumps" }),
      "grundfos-lifting": freezeMapping({ categoryId: "sewage-lifting-units" }),
      "grundfos-pressure-tanks": freezeMapping({ categoryId: "pressure-tanks" }),
      "grundfos-pump-automation": freezeMapping({ categoryId: "pump-automation" }),
      "grundfos-pump-accessories": freezeMapping({ categoryId: "pump-accessories" }),
      "grundfos-pump-services": freezeMapping({ categoryId: "pump-services" }),
      "grundfos-heating-automation": freezeMapping({ categoryId: "heating-controllers" }),
      "grundfos-heating-components": freezeMapping({ categoryId: "branded-heating-accessories" })
    }),
    tekkhaus: Object.freeze({
      "tekkhaus-circulation": freezeMapping({ categoryId: "system-circulation-pumps" }),
      "tekkhaus-surface": freezeMapping({ categoryId: "surface-pumps" }),
      "tekkhaus-pressure": freezeMapping({ categoryId: "pressure-boosting" }),
      "tekkhaus-borehole": freezeMapping({ categoryId: "borehole-pumps" }),
      "tekkhaus-drainage": freezeMapping({ categoryId: "drainage-pumps" }),
      "tekkhaus-sewage": freezeMapping({ categoryId: "sewage-pumps" }),
      "tekkhaus-pool": freezeMapping({ categoryId: "pool-pumps-filtration" }),
      "tekkhaus-pressure-tanks": freezeMapping({ categoryId: "pressure-tanks" }),
      "tekkhaus-automation": freezeMapping({ categoryId: "pump-automation" }),
      "tekkhaus-accessories": freezeMapping({ categoryId: "pump-accessories" }),
      "tekkhaus-feed-grinders": freezeMapping({ categoryId: "feed-grinders" })
    }),
    tech: Object.freeze({
      "tech-automation": freezeMapping({ classifier: "tech-automation", mappingStatus: "classifier" }),
      "tech-sinum": freezeMapping({ classifier: "tech-sinum", collectionIds: ["sinum"], tags: ["sinum"], mappingStatus: "classifier" }),
      "tech-accessories": freezeMapping({ categoryId: "heating-sensors-modules" })
    }),
    altep: Object.freeze({
      "altep-solid-fuel-boilers": freezeMapping({ categoryId: "solid-fuel-boilers" }),
      "altep-pellet-boilers": freezeMapping({ categoryId: "pellet-boilers" }),
      "altep-heat-accumulators": freezeMapping({ categoryId: "heat-accumulators" }),
      "altep-pellet-burners": freezeMapping({ categoryId: "pellet-burners" }),
      "altep-boiler-accessories": freezeMapping({ classifier: "boiler-accessory", mappingStatus: "classifier" }),
      "altep-industrial-heating": freezeMapping({ classifier: "industrial-heating", mappingStatus: "classifier" })
    }),
    feniks: Object.freeze({
      "feniks-solid-fuel-boilers": freezeMapping({ categoryId: "solid-fuel-boilers" }),
      "feniks-pellet-boilers": freezeMapping({ categoryId: "pellet-boilers" }),
      "feniks-heat-accumulators": freezeMapping({ categoryId: "heat-accumulators" }),
      "feniks-pellet-burners": freezeMapping({ categoryId: "pellet-burners" }),
      "feniks-boiler-accessories": freezeMapping({ classifier: "boiler-accessory", mappingStatus: "classifier" }),
      "feniks-industrial-heating": freezeMapping({ classifier: "industrial-heating", mappingStatus: "classifier" })
    }),
    focus: Object.freeze({
      "focus-solid-fuel-boilers": freezeMapping({ categoryId: "solid-fuel-boilers" }),
      "focus-pellet-boilers": freezeMapping({ categoryId: "pellet-boilers" }),
      "focus-heat-accumulators": freezeMapping({ categoryId: "heat-accumulators" }),
      "focus-pellet-burners": freezeMapping({ categoryId: "pellet-burners" }),
      "focus-boiler-accessories": freezeMapping({ classifier: "boiler-accessory", mappingStatus: "classifier" }),
      "focus-industrial-heating": freezeMapping({ classifier: "industrial-heating", mappingStatus: "classifier" }),
      "focus-humidification": freezeMapping({ classifier: "humidification", mappingStatus: "classifier" })
    }),
    baxi: Object.freeze({
      "baxi-gas-boilers": freezeMapping({ categoryId: "gas-boilers" }),
      "baxi-industrial-heating": freezeMapping({ categoryId: "gas-boilers" }),
      "baxi-heat-pumps": freezeMapping({ categoryId: "heat-pumps" }),
      "baxi-hot-water-tanks": freezeMapping({ categoryId: "hot-water-tanks" }),
      "baxi-heat-accumulators": freezeMapping({ categoryId: "heat-accumulators" }),
      "baxi-solar-thermal": freezeMapping({ categoryId: "solar-thermal" }),
      "baxi-automation": freezeMapping({ categoryId: "heating-controllers" }),
      "baxi-boiler-accessories": freezeMapping({ classifier: "boiler-accessory", mappingStatus: "classifier" }),
      "baxi-flue-systems": freezeMapping({ categoryId: "flue-systems" }),
      "baxi-air-conditioners": freezeMapping({ categoryId: "air-conditioners" })
    }),
    buderus: Object.freeze({
      "buderus-gas-boilers": freezeMapping({ categoryId: "gas-boilers" }),
      "buderus-heat-pumps": freezeMapping({ categoryId: "heat-pumps" }),
      "buderus-hot-water-tanks": freezeMapping({ categoryId: "hot-water-tanks" }),
      "buderus-heat-accumulators": freezeMapping({ categoryId: "heat-accumulators" }),
      "buderus-solar-thermal": freezeMapping({ categoryId: "solar-thermal" }),
      "buderus-automation": freezeMapping({ categoryId: "heating-controllers" })
    })
  });

  const supplierLabelMappings = Object.freeze({
    ecosoft: Object.freeze({
      "Фільтри зворотного осмосу": "reverse-osmosis",
      "Проточні фільтри": "flow-filters",
      "Магістральні фільтри": "mainline-filters-housings",
      "Картриджі для фільтрів води": "drinking-system-cartridges",
      "Картриджі магістральні": "mainline-cartridges",
      "Матеріали для фільтрів": "filter-media",
      "Для кафе, ресторанів, готелів": "reverse-osmosis"
    })
  });

  function classifyEcosoftTreatment(product) {
    const sku = String(product.sku || "").toLocaleLowerCase("en");
    const haystack = `${product.sku || ""} ${product.title || ""} ${product.description || ""}`.toLocaleLowerCase("uk");
    if (/^fpa\d/.test(sku)) return "chlorine-odor-removal";
    if (/^fpc\d/.test(sku)) return "chlorine-odor-removal";
    if (/^fp\d/.test(sku)) return "mechanical-treatment";
    if (/^fk/.test(sku)) return "complex-treatment";
    if (/^fu/.test(sku)) return "water-softening";
    if (/видален.*хлор|^fpa\d/i.test(haystack)) return "chlorine-odor-removal";
    if (/сірковод|запах|^fpc\d/i.test(haystack)) return "chlorine-odor-removal";
    if (/механічн|^fp\d/i.test(haystack)) return "mechanical-treatment";
    if (/комплексн|знезалізнення та пом.?якш|ecomix|\bfk\s?\d|^fk/i.test(haystack)) return "complex-treatment";
    if (/пом.?якш|\bfu\s?\d|^fu/i.test(haystack)) return "water-softening";
    if (/знезаліз|заліз|марган/i.test(haystack)) return "iron-removal";
    return "complex-treatment";
  }

  const productText = product => `${product.title || ""} ${product.type || ""} ${product.series || ""}`.toLocaleLowerCase("uk");

  function classifyTermojetPumps(product) {
    const text = productText(product);
    if (/каналізаці|grandlift|\bwt\s?400/.test(text)) return "sewage-lifting-units";
    if (/рециркуляц|гарячого водопостачання|\bгвп\b|\bspe12\b/.test(text)) return "dhw-circulation-pumps";
    return "system-circulation-pumps";
  }

  function classifyHeatingAutomation(product) {
    const text = productText(product);
    if (/кімнатн|термостат|терморегулятор|програматор/.test(text)) return "room-thermostats";
    if (/зональн|термостатичн.*привод|тепл.*підлог|контур/.test(text)) return "zone-control";
    if (/датчик|модул|ретранслятор|антен|інтернет|wi-?fi|кабел|перифер/.test(text)) return "heating-sensors-modules";
    return "heating-controllers";
  }

  function classifyTechAutomation(product) {
    if (/уф-ламп|\bauv[-\s]/.test(productText(product))) return "uv-disinfection";
    return classifyHeatingAutomation(product);
  }

  function classifyTechSinum(product) {
    const text = productText(product);
    if (/ролет|жалюзі/.test(text)) return "smart-shutters";
    if (/вимикач|димер|диммер|освітлен|rgb|світлодіод/.test(text)) return "smart-lighting";
    if (/розетк|реле|din-?рейк|вхідн.*плат|вхідн.*карт|вихідн.*модул/.test(text)) return "smart-relays";
    if (/датчик|мультисенсор|затоплен|дим|частинок|повітр|озонатор|стерилізатор/.test(text)) return "smart-sensors";
    if (/термостат|терморегулятор|температур|фанкойл|клапан|клімат/.test(text)) return "smart-climate";
    if (/живлен|кабел|рамк|ретранслятор|антен|монтаж/.test(text)) return "smart-accessories";
    return "smart-home-hubs";
  }

  function classifyHeatingComponent(product) {
    const text = productText(product);
    if (/комплект підключення|муфт|перехід|закінчен|адаптер/.test(text)) return "heating-fittings";
    if (/насосна група|\bbox\d/.test(text)) return "pump-groups";
    if (/гідростріл|гідравлічн.*розділ/.test(text)) return "hydraulic-separators";
    if (/колектор/.test(text)) return "distribution-manifolds";
    return "branded-heating-accessories";
  }

  function classifyBoilerAccessory(product) {
    const text = productText(product);
    if (/двер|лутк|рам.*кот|колосник|зачеп/.test(text)) return "boiler-doors-grates";
    if (/бункер|шнек|подач.*палив/.test(text)) return "pellet-hoppers-augers";
    if (/вентилятор/.test(text)) return "boiler-fans";
    if (/пневмоочищ|систем.*очищ/.test(text)) return "boiler-cleaning-systems";
    if (/гідравлічн.*вузол|гідровузол/.test(text)) return "boiler-hydraulic-nodes";
    return "branded-heating-accessories";
  }

  function classifyIndustrialHeating(product) {
    return /парогенератор/.test(productText(product)) ? "steam-generators" : "heat-generators";
  }

  function classifyHumidification(product) {
    return /комплектуюч/.test(productText(product)) ? "misting-components" : "misting-systems";
  }

  const classifiers = Object.freeze({
    "ecosoft-treatment": classifyEcosoftTreatment,
    "termojet-pumps": classifyTermojetPumps,
    "heating-automation": classifyHeatingAutomation,
    "tech-automation": classifyTechAutomation,
    "tech-sinum": classifyTechSinum,
    "heating-component": classifyHeatingComponent,
    "boiler-accessory": classifyBoilerAccessory,
    "industrial-heating": classifyIndustrialHeating,
    humidification: classifyHumidification
  });

  function supplierFor(product) {
    if (String(product.brand || "").toLocaleLowerCase("en") === "buderus") return "buderus";
    if (String(product.brand || "").toLocaleLowerCase("en") === "baxi") return "baxi";
    if (String(product.brand || "").toLocaleLowerCase("en") === "focus") return "focus";
    if (String(product.brand || "").toLocaleLowerCase("en") === "feniks") return "feniks";
    if (String(product.brand || "").toLocaleLowerCase("en") === "altep") return "altep";
    if (String(product.brand || "").toLocaleLowerCase("en") === "tech") return "tech";
    if (String(product.brand || "").toLocaleLowerCase("en").replace(/[^a-z]/g, "") === "tekkhaus") return "tekkhaus";
    if (String(product.brand || "").toLocaleLowerCase("en") === "grundfos") return "grundfos";
    if (String(product.brand || "").toLocaleLowerCase("en") === "wilo") return "wilo";
    if (String(product.brand || "").toLocaleLowerCase("en") === "ecosoft" || product.category === "water") return "ecosoft";
    if (String(product.brand || "").toLocaleLowerCase("en") === "termojet" || product.category === "heating") return "termojet";
    return "";
  }

  function sourceCategoryFor(product, supplier = supplierFor(product)) {
    if (supplier === "termojet") return product.primaryCategory || `termojet-${product.typeSlug || ""}`;
    return product.sourceCategoryId || product.typeSlug || "";
  }

  function resolve(product, supplier = supplierFor(product)) {
    const sourceCategory = sourceCategoryFor(product, supplier);
    const configured = categoryMappings[supplier]?.[sourceCategory];
    if (!configured) return Object.freeze({ supplier, sourceCategory, categoryId: "", seriesId: null, collectionIds: Object.freeze([]), tags: Object.freeze([]), mappingStatus: "unmapped" });
    const classifier = configured.classifier || (configured.mappingStatus === "classifier" && supplier === "ecosoft" ? "ecosoft-treatment" : "");
    const categoryId = classifier ? classifiers[classifier]?.(product) || "" : configured.categoryId;
    return Object.freeze({ ...configured, supplier, sourceCategory, categoryId });
  }

  window.sofievkaSourceMappings = Object.freeze({
    suppliers,
    categoryMappings,
    supplierLabelMappings,
    classifyEcosoftTreatment,
    classifiers,
    supplierFor,
    sourceCategoryFor,
    resolve
  });
})();
/* Source: catalog/normalize-products.js */
(function () {
  "use strict";

  const taxonomy = window.sofievkaTaxonomy;
  const attributeSchema = window.sofievkaAttributeSchema;
  const sourceMappings = window.sofievkaSourceMappings;
  if (!taxonomy || !attributeSchema || !sourceMappings) throw new Error("Catalog foundations must load before product normalization.");

  const slugify = value => String(value || "")
    .toLocaleLowerCase("uk")
    .normalize("NFKD")
    .replace(/[\u0300-\u036f]/g, "")
    .replace(/[^a-z0-9а-яіїєґ]+/giu, "-")
    .replace(/^-+|-+$/g, "");

  const officialEcosoftFacts = Object.freeze({
    MO3400PECO: [["Продуктивність", "60 л/год"], ["ККД", "50%"], ["Формат", "Прямоточний, без бака"]],
    MO1500PECO: [["Продуктивність", "78 л/год"], ["ККД", "50%"], ["Формат", "Прямоточний, без бака"]],
    MO2800PECO: [["Продуктивність", "120 л/год"], ["ККД", "67%"], ["Формат", "Прямоточний, без бака"]],
    MO3600PECO: [["Продуктивність", "90 л/год"], ["ККД", "50–65%"], ["Формат", "Прямоточний, без бака"]],
    MO3600MPECO: [["Продуктивність", "90 л/год"], ["Мінералізація", "40–70 мг/л"], ["ККД", "60%"]],
    MO675MPUREBALECO: [["Продуктивність", "12 л/год"], ["Мінералізація", "Кальцій і магній"], ["ККД", "50%"]],
    MO675MBALPSECO: [["Продуктивність", "12 л/год"], ["Мінералізація", "Кальцій і магній"], ["Помпа", "Є"]],
    MO675PUREMACECO: [["Продуктивність", "12 л/год"], ["Мінералізація", "Кальцій"], ["ККД", "50%"]],
    MO675PSMACECO: [["Продуктивність", "12 л/год"], ["Мінералізація", "Кальцій"], ["Помпа", "Є"]],
    MO675MECO: [["Продуктивність", "8 л/год"], ["Мінералізація", "20–30 мг/л"], ["ККД", "35%"]],
    MO675MPSECO: [["Продуктивність", "8 л/год"], ["Мінералізація", "20–30 мг/л"], ["Помпа", "Є"]],
    MO550MECOSTD: [["Продуктивність", "8 л/год"], ["Мінералізація", "20–30 мг/л"], ["ККД", "35%"]],
    MO650MECOSTD: [["Продуктивність", "8 л/год"], ["Мінералізація", "20–30 мг/л"], ["ККД", "35%"]],
    MO550MPECOSTD: [["Продуктивність", "8 л/год"], ["Мінералізація", "20–30 мг/л"], ["Помпа", "Є"]],
    ROBUST1000STD: [["Продуктивність", "55–60 л/год"], ["Застосування", "Невеликі кафе й офіси"], ["Режим роботи", "До 20 год/добу"]],
    ROBUST1500ECO: [["Продуктивність", "90–100 л/год"], ["Застосування", "Кафе, готелі, лабораторії"], ["Режим роботи", "До 20 год/добу"]],
    ROBUSTCOFFEE: [["Продуктивність", "70–75 л/год"], ["Застосування", "Вода для кави за SCA"], ["Режим роботи", "До 20 год/добу"]],
    ROBUST3000MAX: [["Продуктивність", "150–160 л/год"], ["Застосування", "Ресторани й обладнання HoReCa"], ["Режим роботи", "До 20 год/добу"]],
    ROBUST4000: [["Продуктивність", "150–180 л/год"], ["Застосування", "Професійні посудомийні машини"], ["Формат", "Прямоточний"]]
  });

  const editorialCorrections = Object.freeze({
    "termojet-wp_20506": "Високоефективний циркуляційний насос SPE12 для рециркуляції води в системах гарячого водопостачання. Компактний корпус і тиха робота підходять для побутового використання.",
    "termojet-wp_20502": "Високоефективний циркуляційний насос SPE12 з таймером і термостатом для рециркуляції води в системах гарячого водопостачання. Компактний корпус і тиха робота підходять для побутового використання."
  });

  function cleanSupplierText(value) {
    return String(value || "")
      .replace(/\*+$/g, "")
      .replace(/мінеральнихолій/giu, "мінеральних олій")
      .replace(/характеристиками,близькими/giu, "характеристиками, близькими")
      .replace(/ізмагнітним/giu, "із магнітним")
      .trim();
  }

  function applyEditorialCorrections(product, category) {
    const correctedDescription = editorialCorrections[product.id] || cleanSupplierText(product.description);
    const correctedType = /^акція$/iu.test(String(product.type || "")) ? category.title : product.type || category.title;
    return {
      ...product,
      type: correctedType,
      description: correctedDescription,
      shortDescription: editorialCorrections[product.id] || cleanSupplierText(product.shortDescription || correctedDescription),
      fullDescription: editorialCorrections[product.id] || cleanSupplierText(product.fullDescription || correctedDescription)
    };
  }

  function pushFact(facts, origins, label, value, provenance = "derived", rule = "title-description") {
    if (!value || facts.some(([existingLabel]) => existingLabel === label)) return;
    facts.push([label, value]);
    origins[label] = Object.freeze({ provenance, rule });
  }

  function ecosoftDetails(product, categoryId, categoryTitle) {
    const source = `${product.title || ""} ${product.description || ""}`.replace(/[“”]/g, '"');
    const lower = source.toLocaleLowerCase("uk");
    const facts = [];
    const origins = {};
    (officialEcosoftFacts[product.sku] || []).forEach(([label, value]) => pushFact(facts, origins, label, value, "source-confirmed", "ecosoft-official-2026"));
    const size = source.match(/(\d+(?:[.,]\d+)?["″”]\s*[xх×]\s*\d+(?:[.,]\d+)?["″”]?)/i)?.[1] || source.match(/(\d+(?:[.,]\d+)?["″”])/i)?.[1];
    const microns = source.match(/(\d+(?:[–-]\d+)?)\s*мкм/i)?.[1];
    const packageQuantity = String(product.title || "").match(/(\d+)\s*шт\.?/i)?.[1];
    const period = String(product.title || "").match(/(\d+)\s*місяц/i)?.[1];
    const massOrVolume = String(product.title || "").match(/(\d+(?:[.,]\d+)?)\s*(кг|л|мл)/i);
    const gpd = String(product.title || "").match(/(\d+)\s*GPD/i)?.[1];
    const connection = String(product.title || "").match(/\b(1\/2|3\/4|1)["″”]/)?.[1];

    if (categoryId === "reverse-osmosis") {
      pushFact(facts, origins, "Технологія", "Зворотний осмос");
      pushFact(facts, origins, "Встановлення", /настільн/i.test(lower) ? "Настільне" : "Під мийку");
      pushFact(facts, origins, "Формат", /cross|прямоточ|robust/i.test(lower) ? "Прямоточний, без бака" : "З накопичувальним баком");
    } else if (categoryId === "flow-filters") {
      pushFact(facts, origins, "Тип", "Триступеневий проточний фільтр");
      pushFact(facts, origins, "Встановлення", "Під мийку");
      pushFact(facts, origins, "Призначення", "Доочищення питної води");
    } else if (categoryId === "drinking-system-cartridges") {
      pushFact(facts, origins, "Тип", /мембран/i.test(lower) ? "Мембранний елемент" : /комплект|запас/i.test(lower) ? "Комплект змінних елементів" : /мінераліз/i.test(lower) ? "Мінералізатор" : "Змінний картридж");
      pushFact(facts, origins, "Сумісність", /pure aquacalcium/i.test(lower) ? "PURE AquaCalcium" : /pure balance/i.test(lower) ? "PURE Balance" : /absolute/i.test(lower) ? "Absolute" : /standard pro/i.test(lower) ? "Standard PRO" : /standard/i.test(lower) ? "Standard" : /потрійн/i.test(lower) ? "Потрійні фільтри" : "Системи зворотного осмосу");
      if (gpd) pushFact(facts, origins, "Продуктивність мембрани", `${gpd} GPD`);
      if (period) pushFact(facts, origins, "Комплект на", `${period} місяців`);
    } else if (categoryId === "mainline-cartridges") {
      pushFact(facts, origins, "Призначення", /сірковод/i.test(lower) ? "Видалення сірководню" : /заліз/i.test(lower) ? "Видалення заліза" : /пом.?якш/i.test(lower) ? "Пом’якшення води" : /вугіл|карбон|cto/i.test(lower) ? "Видалення хлору й запахів" : "Механічне очищення");
      if (size) pushFact(facts, origins, "Формат", size.replace(/[xх]/gi, "×"));
      if (microns) pushFact(facts, origins, "Тонкість фільтрації", `${microns} мкм`);
    } else if (categoryId === "mainline-filters-housings") {
      pushFact(facts, origins, "Призначення", /накип/i.test(lower) ? "Захист від накипу" : "Механічне очищення");
      pushFact(facts, origins, "Вода", /гаряч/i.test(lower) ? "Гаряча" : "Холодна");
      if (connection) pushFact(facts, origins, "Підключення", `${connection}″`);
    } else if (categoryId === "filter-media") {
      pushFact(facts, origins, "Тип", /сіль/i.test(lower) ? "Сіль для регенерації" : /вугіл/i.test(lower) ? "Активоване вугілля" : /смол/i.test(lower) ? "Іонообмінна смола" : /пісок/i.test(lower) ? "Кварцова підкладка" : "Фільтрувальне завантаження");
      if (massOrVolume) pushFact(facts, origins, "Фасування", `${massOrVolume[1].replace(",", ".")} ${massOrVolume[2]}`);
      pushFact(facts, origins, "Застосування", /сіль/i.test(lower) ? "Регенерація пом’якшувачів" : "Засипні системи очищення");
    } else {
      pushFact(facts, origins, "Призначення", categoryTitle);
      const users = lower.match(/(?:до|від)\s+(\d+(?:[-–]\d+)?)\s+(?:осіб|користувач)/i)?.[1];
      const bathrooms = lower.match(/(\d+(?:[-–]\d+)?)\s*санвуз/i)?.[1];
      if (users) pushFact(facts, origins, "Для сім’ї", `${users} ос.`);
      if (bathrooms) pushFact(facts, origins, "Кількість санвузлів", bathrooms);
      pushFact(facts, origins, "Встановлення", /компактн/i.test(lower) ? "Компактний корпус" : "Колонна система");
    }
    if (packageQuantity) pushFact(facts, origins, "У комплекті", `${packageQuantity} шт.`);
    pushFact(facts, origins, "Категорія", categoryTitle, "mapped", "central-taxonomy");
    return { facts: facts.slice(0, 8), origins };
  }

  function ecosoftFeatures(product, categoryId) {
    const text = `${product.title || ""} ${product.description || ""}`.toLocaleLowerCase("uk");
    const features = {};
    const origins = {};
    const set = (id, value, rule) => { if (value !== "" && value !== null && value !== undefined) { features[id] = value; origins[id] = Object.freeze({ provenance: "derived", rule }); } };
    if (/без помп/.test(text)) set("pump", "no", "negative-title-pattern");
    else if (/помп|насос підвищення тиску/.test(text)) set("pump", "yes", "title-description-pattern");
    if (/без мінераліз/.test(text)) set("mineralizer", "no", "negative-title-pattern");
    else if (/мінераліз/.test(text)) set("mineralizer", "yes", "title-description-pattern");
    if (/прямоточ|cross solo|cross 90|robust/.test(text)) set("flowType", "direct", "title-description-pattern");
    else if (/накопичувальн.*бак/.test(text)) set("flowType", "tank", "title-description-pattern");
    if (/квартир/.test(text)) set("scope", "apartment", "title-description-pattern");
    else if (/будин|котедж/.test(text)) set("scope", "house", "title-description-pattern");
    if (product.typeSlug === "horeca" || /кафе|ресторан|готел|horeca|coffee/.test(text)) set("scope", "business", "supplier-category-or-text-pattern");
    if (/свердлов|артезіан/.test(text)) set("waterSource", "borehole", "title-description-pattern");
    else if (/водопровід|водогін/.test(text)) set("waterSource", "mains", "title-description-pattern");
    if (["reverse-osmosis", "flow-filters"].includes(categoryId)) set("installation", "under-sink", "category-rule");
    const purposeByCategory = { "mechanical-treatment": "mechanical", "water-softening": "softening", "chlorine-odor-removal": "chlorine-odor", "complex-treatment": "complex" };
    if (purposeByCategory[categoryId]) set("purpose", purposeByCategory[categoryId], "category-rule");
    return { features, origins };
  }

  function readableEcosoftDescription(product, categoryId, categoryTitle) {
    const title = String(product.title || "").toLocaleLowerCase("uk");
    if (/robust/i.test(title)) return "Прямоточна система зворотного осмосу для професійного використання у кафе, ресторанах, готелях та інших закладах.";
    if (categoryId === "reverse-osmosis") return /cross/i.test(title) ? "Компактна прямоточна система зворотного осмосу для щоденного очищення питної води без накопичувального бака." : `Система зворотного осмосу для питної води${/помп/i.test(title) ? " зі стабільною роботою за низького вхідного тиску" : " з накопичувальним баком"}${/мінераліз|balance|aquacalcium/i.test(title) ? " та мінералізацією" : ""}.`;
    if (categoryId === "flow-filters") return "Триступеневий проточний фільтр для доочищення питної води під кухонною мийкою.";
    if (categoryId === "drinking-system-cartridges") return /комплект|запас/i.test(title) ? "Комплект змінних елементів для планового обслуговування сумісної питної системи Ecosoft." : "Змінний елемент для відновлення якості очищення у сумісній питній системі Ecosoft.";
    if (categoryId === "mainline-cartridges") return /вугіл|карбон|cto/i.test(title) ? "Магістральний картридж для зниження вмісту хлору, стороннього запаху та органічних домішок у воді." : /заліз/i.test(title) ? "Магістральний картридж для зниження вмісту заліза у воді." : /сірковод/i.test(title) ? "Магістральний картридж для зниження вмісту сірководню та неприємного запаху." : "Картридж для механічного очищення води від піску, мулу, іржі та інших нерозчинних домішок.";
    if (categoryId === "mainline-filters-housings") return /накип/i.test(title) ? "Компактне рішення для захисту побутової техніки та нагрівального обладнання від утворення накипу." : "Магістральний фільтр або корпус для попереднього механічного очищення води у квартирі чи будинку.";
    if (categoryId === "filter-media") return "Матеріал для завантаження або регенерації засипної системи очищення води. Тип і кількість підбирають за складом води та параметрами фільтра.";
    return `Система Ecosoft для категорії «${categoryTitle.toLocaleLowerCase("uk")}». Підбір моделі виконується за аналізом води, піковою витратою та умовами монтажу.`;
  }

  function sourceAttributeEntries(product) {
    return (Array.isArray(product.attributes) ? product.attributes : []).map(attribute => Array.isArray(attribute)
      ? { label: cleanSupplierText(attribute[0]).replace(/\*+(?=:|$)/g, ""), value: cleanSupplierText(attribute[1]), origin: "supplier" }
      : { label: cleanSupplierText(attribute?.label).replace(/\*+(?=:|$)/g, ""), value: cleanSupplierText(attribute?.value), origin: "supplier" }).filter(item => item.label && item.value);
  }

  function detailEntries(product) {
    const technical = (Array.isArray(product.technicalDetails) ? product.technicalDetails : []).map(entry => ({
      label: String(entry?.[0] || "").trim(),
      value: String(entry?.[1] || "").trim(),
      origin: product.technicalDetailOrigins?.[entry?.[0]]?.provenance || "supplier",
      rule: product.technicalDetailOrigins?.[entry?.[0]]?.rule || "supplier-attribute"
    }));
    const combined = [...sourceAttributeEntries(product), ...technical];
    const seen = new Set();
    return combined.filter(item => item.label && item.value && !seen.has(`${item.label}\u0000${item.value}`) && seen.add(`${item.label}\u0000${item.value}`));
  }

  function matchesAlias(label, definition) {
    return definition.aliases.some(alias => alias instanceof RegExp ? alias.test(label) : String(alias).toLocaleLowerCase("uk") === label.toLocaleLowerCase("uk"));
  }

  function parseNumber(value, label, definition) {
    if (typeof value === "number" && Number.isFinite(value)) return { value: Number(value.toFixed(4)), unitStatus: "normalized-feature" };
    const normalized = String(value || "").replace(/,/g, ".").replace(/\s+/g, " ");
    if (/\d+(?:\.\d+)?\s*[-–]\s*\d+(?:\.\d+)?/.test(normalized)) return { value: null, unitStatus: "range-preserved" };
    const match = normalized.match(/-?\d+(?:\.\d+)?/);
    if (!match) return { value: null, unitStatus: "malformed" };
    let number = Number(match[0]);
    const unitText = `${label} ${normalized}`.toLocaleLowerCase("uk");
    const unitPatterns = {
      "кВт": /квт|(^|\s)вт\b/i,
      "м³/год": /м[³3]\s*\/\s*год|л\s*\/\s*(?:хв|мин|год)|^kvs?\b/i,
      "м": /(^|\s)м(?:\s|$)/i,
      "мм": /мм|mm|(^|\s)см(?:\s|$)/i,
      "бар": /бар|bar|мпа/i,
      "л": /(^|\s)л(?:\s|$)/i,
      "л/год": /л\s*\/\s*год|м[³3]\s*\/\s*год/i,
      "кг": /кг|kg/i,
      "мкм": /мкм|micron/i
    };
    if (definition.unit && unitPatterns[definition.unit] && !unitPatterns[definition.unit].test(unitText)) return { value: null, unitStatus: "unknown-unit" };
    if (definition.unit === "кВт" && /(^|\s)вт\b/i.test(unitText) && !/квт/i.test(unitText)) number /= 1000;
    else if (definition.unit === "м³/год" && /л\s*\/\s*хв|л\/мин/i.test(unitText)) number *= 0.06;
    else if (definition.unit === "м³/год" && /л\s*\/\s*год/i.test(unitText)) number /= 1000;
    else if (definition.unit === "бар" && /мпа/i.test(unitText)) number *= 10;
    else if (definition.unit === "мм" && /(^|\s)см\b/i.test(unitText)) number *= 10;
    else if (definition.unit === "л/год" && /м³\s*\/\s*год/i.test(unitText)) number *= 1000;
    return Number.isFinite(number) ? { value: Number(number.toFixed(4)), unitStatus: "normalized" } : { value: null, unitStatus: "malformed" };
  }

  function normalizeAttributes(product, supplier, category) {
    const entries = detailEntries(product);
    const normalized = {};
    const records = [];
    const usedEntries = new Set();
    const featureValues = product.features || {};
    const featureOrigins = product.featureOrigins || {};

    Object.entries(attributeSchema.definitions).forEach(([id, definition]) => {
      let value = featureValues[id];
      let provenance = featureOrigins[id]?.provenance || (value !== undefined ? (supplier === "ecosoft" ? "derived" : "source-confirmed") : "");
      let rule = featureOrigins[id]?.rule || (value !== undefined ? "normalized-feature" : "");
      let sourceLabel = "";
      let sourceValue = "";
      if (id === "productType") {
        value = product.type || category.title;
        provenance = "mapped";
        rule = "supplier-category-mapping";
      }
      if (value === undefined || value === null || value === "") {
        const index = entries.findIndex(entry => !usedEntries.has(entry) && matchesAlias(entry.label, definition));
        if (index >= 0) {
          const entry = entries[index];
          usedEntries.add(entry);
          value = entry.value;
          sourceLabel = entry.label;
          sourceValue = entry.value;
          provenance = entry.origin === "supplier" ? "source-confirmed" : entry.origin;
          rule = entry.rule || "attribute-alias";
        }
      }
      if (value === undefined || value === null || value === "") return;
      let unitStatus = "not-applicable";
      if (definition.type === "number") {
        const parsed = parseNumber(value, sourceLabel, definition);
        if (parsed.value === null) return;
        value = parsed.value;
        unitStatus = parsed.unitStatus;
      }
      entries.filter(entry => matchesAlias(entry.label, definition)).forEach(entry => usedEntries.add(entry));
      normalized[id] = value;
      records.push(Object.freeze({ id, label: definition.label, value, unit: definition.unit || "", provenance, rule, sourceLabel, sourceValue, unitStatus }));
    });

    const ignoredLabels = [/^категорія$/i, /^категорія виробника$/i, /^офіційна назва$/i, /^бренд$/i, /^стан$/i, /^наявність$/i, /^артикул/i];
    const unmapped = entries.filter(entry => !usedEntries.has(entry) && !ignoredLabels.some(pattern => pattern.test(entry.label))).map(entry => Object.freeze({ label: entry.label, value: entry.value, provenance: entry.origin === "supplier" ? "source-confirmed" : entry.origin }));
    return { normalized: Object.freeze(normalized), records: Object.freeze(records), unmapped: Object.freeze(unmapped) };
  }

  function enrichEcosoft(product, mapping) {
    const category = taxonomy.byId[mapping.categoryId];
    const featureData = ecosoftFeatures(product, mapping.categoryId);
    const detailData = ecosoftDetails(product, mapping.categoryId, category.title);
    const description = String(product.description || "").trim() || readableEcosoftDescription(product, mapping.categoryId, category.title);
    const slug = product.slug || product.link?.split("/").filter(Boolean).pop() || product.id;
    return {
      ...product,
      slug: slugify(slug),
      shortTitle: String(product.title || "").replace(/\s+Ecosoft\s+/i, " ").replace(/\s{2,}/g, " ").trim(),
      shortDescription: description,
      fullDescription: description,
      supplierCategory: product.type,
      sourceUrl: "",
      officialSourceUrl: "",
      officialCatalogEdition: "Актуальні дані виробника",
      features: Object.freeze(featureData.features),
      featureOrigins: Object.freeze(featureData.origins),
      technicalDetails: Object.freeze(detailData.facts.map(item => Object.freeze(item))),
      technicalDetailOrigins: Object.freeze(detailData.origins),
      cardFacts: Object.freeze(detailData.facts.slice(0, 3).map(item => Object.freeze(item))),
      variants: Object.freeze([]),
      compatibleProducts: Object.freeze([]),
      compatibleConsumables: Object.freeze([]),
      documents: Object.freeze(Array.isArray(product.documents) ? [...product.documents] : []),
      instructions: Object.freeze(Array.isArray(product.instructions) ? [...product.instructions] : [])
    };
  }

  function normalizeProduct(rawProduct) {
    const supplier = sourceMappings.supplierFor(rawProduct);
    const mapping = sourceMappings.resolve(rawProduct, supplier);
    const category = taxonomy.byId[mapping.categoryId];
    if (!supplier || !category) return Object.freeze({ ...rawProduct, normalizationError: !supplier ? "unknown-supplier" : "unmapped-category" });
    const enrichedProduct = supplier === "ecosoft" ? enrichEcosoft(rawProduct, mapping) : rawProduct;
    const product = applyEditorialCorrections(enrichedProduct, category);
    const supplierConfig = sourceMappings.suppliers[supplier];
    const brandId = supplierConfig.brandId;
    const attributeResult = normalizeAttributes(product, supplier, category);
    const sourceAttributes = Object.freeze(sourceAttributeEntries(rawProduct).map(item => Object.freeze({ label: item.label, value: item.value })));
    const imageOrigin = supplierConfig.imageOrigin || "";
    const resolveImage = value => typeof value === "string" && imageOrigin && value.startsWith("/images/") ? `${imageOrigin}${value}` : value;
    const images = Object.freeze((Array.isArray(product.images) && product.images.length ? [...product.images] : [product.image].filter(Boolean)).map(resolveImage));
    const tags = Object.freeze([...new Set([...(Array.isArray(product.tags) ? product.tags : []), ...mapping.tags])]);
    const collections = Object.freeze([...new Set([...(Array.isArray(product.collections) ? product.collections : []), ...mapping.collectionIds])]);
    const amount = Number(product.price);
    const inventoryStatus = product.availability || "unknown";
    return Object.freeze({
      ...product,
      id: product.id,
      sku: product.sku,
      slug: slugify(product.slug || product.link?.split("/").filter(Boolean).pop() || product.id),
      model: product.model || product.title,
      seriesId: mapping.seriesId || product.seriesId || null,
      brandId,
      primaryCategoryId: category.id,
      secondaryCategoryIds: Object.freeze(Array.isArray(product.secondaryCategoryIds) ? [...product.secondaryCategoryIds] : []),
      sectionId: category.sectionId,
      sourceCategoryId: mapping.sourceCategory,
      sourceCategoryName: product.primaryCategoryName || product.type || mapping.sourceCategory,
      primaryCategory: category.id,
      primaryCategoryName: category.title,
      categoryGroup: category.parentId,
      categoryGroupName: taxonomy.byId[category.parentId]?.title || "",
      normalizedType: category.id,
      price: amount,
      pricing: Object.freeze({ amount, currency: product.currency || "UAH" }),
      inventory: Object.freeze({ status: inventoryStatus }),
      stockStatus: inventoryStatus,
      images,
      sourceAttributes,
      attributes: sourceAttributes,
      catalogAttributes: attributeResult.records,
      normalizedAttributes: attributeResult.normalized,
      unmappedAttributes: attributeResult.unmapped,
      tags,
      collections,
      source: Object.freeze({ supplier, sourceId: String(product.id), sourceCategory: mapping.sourceCategory, mappingStatus: mapping.mappingStatus }),
      compareType: category.id,
      badges: Object.freeze(Array.isArray(product.badges) ? [...product.badges] : []),
      image: images[0] || ""
    });
  }

  function normalizeAll(rawProducts) {
    const seenIds = new Set();
    const seenSlugs = new Set();
    const duplicateInputIds = [];
    const adjustedSlugs = [];
    const products = [];
    rawProducts.forEach(rawProduct => {
      if (!rawProduct?.id || seenIds.has(rawProduct.id)) {
        if (rawProduct?.id) duplicateInputIds.push(rawProduct.id);
        return;
      }
      seenIds.add(rawProduct.id);
      let product = normalizeProduct(rawProduct);
      if (!product.normalizationError && seenSlugs.has(product.slug)) {
        const originalSlug = product.slug;
        const uniqueSlug = `${originalSlug}-${slugify(product.id)}`;
        product = Object.freeze({ ...product, slug: uniqueSlug });
        adjustedSlugs.push(Object.freeze({ id: product.id, originalSlug, slug: uniqueSlug }));
      }
      if (product.slug) seenSlugs.add(product.slug);
      products.push(product);
    });
    return Object.freeze({ products: Object.freeze(products), duplicateInputIds: Object.freeze(duplicateInputIds), adjustedSlugs: Object.freeze(adjustedSlugs) });
  }

  const rawWaterProducts = Array.isArray(window.sofievkaRawWaterProducts)
    ? window.sofievkaRawWaterProducts
    : (Array.isArray(window.sofievkaProducts) ? window.sofievkaProducts : []);
  const rawHeatingProducts = Array.isArray(window.sofievkaTermojetProducts) ? window.sofievkaTermojetProducts : [];
  const rawWiloProducts = Array.isArray(window.sofievkaWiloProducts) ? window.sofievkaWiloProducts : [];
  const rawGrundfosProducts = Array.isArray(window.sofievkaGrundfosProducts) ? window.sofievkaGrundfosProducts : [];
  const rawTekkhausProducts = Array.isArray(window.sofievkaTekkhausProducts) ? window.sofievkaTekkhausProducts : [];
  const rawTechProducts = Array.isArray(window.sofievkaTechProducts) ? window.sofievkaTechProducts : [];
  const rawHeatingBrandsProducts = Array.isArray(window.sofievkaHeatingBrandsProducts) ? window.sofievkaHeatingBrandsProducts : [];
  const rawBaxiBuderusProducts = Array.isArray(window.sofievkaBaxiBuderusProducts) ? window.sofievkaBaxiBuderusProducts : [];
  const result = normalizeAll([...rawWaterProducts, ...rawHeatingProducts, ...rawWiloProducts, ...rawGrundfosProducts, ...rawTekkhausProducts, ...rawTechProducts, ...rawHeatingBrandsProducts, ...rawBaxiBuderusProducts]);

  window.sofievkaProductNormalizer = Object.freeze({ slugify, normalizeProduct, normalizeAll });
  window.sofievkaNormalizedProducts = result.products;
  window.sofievkaNormalizationReport = Object.freeze({
    sourceCount: rawWaterProducts.length + rawHeatingProducts.length + rawWiloProducts.length + rawGrundfosProducts.length + rawTekkhausProducts.length + rawTechProducts.length + rawHeatingBrandsProducts.length + rawBaxiBuderusProducts.length,
    normalizedCount: result.products.length,
    waterSourceCount: rawWaterProducts.length,
    heatingSourceCount: rawHeatingProducts.length,
    wiloSourceCount: rawWiloProducts.length,
    grundfosSourceCount: rawGrundfosProducts.length,
    tekkhausSourceCount: rawTekkhausProducts.length,
    techSourceCount: rawTechProducts.length,
    heatingBrandsSourceCount: rawHeatingBrandsProducts.length,
    baxiBuderusSourceCount: rawBaxiBuderusProducts.length,
    duplicateInputIds: result.duplicateInputIds,
    adjustedSlugs: result.adjustedSlugs,
    normalizationErrors: Object.freeze(result.products.filter(product => product.normalizationError).map(product => Object.freeze({ id: product.id, error: product.normalizationError })))
  });
})();
/* Source: catalog/routing.js */
(function () {
  "use strict";

  const taxonomy = window.sofievkaTaxonomy;
  const sourceMappings = window.sofievkaSourceMappings;
  if (!taxonomy) throw new Error("Catalog taxonomy must be initialized before routing.");

  const rootPath = "/catalog";
  const legacySectionAliases = Object.freeze({
    "/heating": "heating",
    "/water-supply": "water-supply",
    "/plumbing": "plumbing",
    "/climate": "climate"
  });
  const legacyQuerySections = Object.freeze({
    heating: "heating",
    water: "water-supply",
    treatment: "water-treatment",
    automation: "smart-home",
    plumbing: "plumbing",
    climate: "climate"
  });
  const legacyCategoryPaths = Object.freeze({
    "/catalog/heating/gas-boilers": "gas-boilers",
    "/catalog/heating/hot-water-tanks": "hot-water-tanks",
    "/catalog/heating/solid-fuel-boilers": "solid-fuel-boilers",
    "/catalog/heating/pellet-boilers": "pellet-boilers",
    "/catalog/heating/heat-accumulators": "heat-accumulators",
    "/catalog/heating/pellet-burners": "pellet-burners",
    "/catalog/heating/boiler-accessories": "heating-components",
    "/catalog/heating/industrial-heating": "heat-generation",
    "/catalog/heating/valves": "heating-valves",
    "/catalog/water-supply/water-treatment": "water-treatment",
    "/catalog/water-supply/water-supply-components": "water-supply-components",
    "/catalog/water-supply/water-supply-components/pump-services": "pump-services",
    "/catalog/water-treatment/reverse-osmosis": "reverse-osmosis",
    "/catalog/water-treatment/flow-filters": "flow-filters",
    "/catalog/water-treatment/mainline-filters-housings": "mainline-filters-housings",
    "/catalog/water-treatment/mainline-cartridges": "mainline-cartridges",
    "/catalog/water-treatment/drinking-system-cartridges": "drinking-system-cartridges",
    "/catalog/water-treatment/filter-media": "filter-media",
    "/catalog/water-treatment/complex-treatment": "complex-treatment",
    "/catalog/water-treatment/water-softening": "water-softening",
    "/catalog/water-treatment/chlorine-odor-removal": "chlorine-odor-removal",
    "/catalog/water-treatment/mechanical-treatment": "mechanical-treatment"
  });

  function normalizePathname(pathname = rootPath) {
    let value = String(pathname || rootPath).split("?")[0] || rootPath;
    try { value = decodeURIComponent(value); } catch { /* Keep a malformed segment for the not-found state. */ }
    value = value.replace(/\.html$/i, "").replace(/\/+$/, "") || "/";
    return value.startsWith("/") ? value : `/${value}`;
  }

  function getCategoryAncestors(categoryId) {
    const result = [];
    let current = taxonomy.byId[categoryId];
    const visited = new Set();
    while (current?.parentId && !visited.has(current.parentId)) {
      visited.add(current.parentId);
      current = taxonomy.byId[current.parentId];
      if (current) result.unshift(current);
    }
    return Object.freeze(result);
  }

  function getCategoryPath(categoryId) {
    const current = taxonomy.byId[categoryId];
    if (!current) return rootPath;
    const chain = [...getCategoryAncestors(categoryId), current];
    return `${rootPath}/${chain.map(category => encodeURIComponent(category.slug)).join("/")}`;
  }

  function categoryContext(currentCategory, extra = {}) {
    const ancestors = currentCategory ? getCategoryAncestors(currentCategory.id) : Object.freeze([]);
    const section = currentCategory ? (ancestors[0] || currentCategory) : null;
    const children = Object.freeze(currentCategory ? taxonomy.childrenOf(currentCategory.id) : taxonomy.childrenOf(null));
    const descendantIds = Object.freeze(currentCategory
      ? [currentCategory.id, ...taxonomy.descendantsOf(currentCategory.id).map(category => category.id)]
      : taxonomy.nodes.map(category => category.id));
    return Object.freeze({
      catalogState: currentCategory ? "category" : "all",
      currentCategory,
      categoryId: currentCategory?.id || "",
      section,
      sectionId: section?.id || "all",
      ancestors,
      children,
      descendantIds,
      canonicalPath: currentCategory ? getCategoryPath(currentCategory.id) : rootPath,
      status: currentCategory?.status || "active",
      notFound: false,
      reason: "",
      legacy: false,
      legacySourceCategoryId: "",
      redirectTo: "",
      ...extra
    });
  }

  function notFoundContext(pathname, reason, matched = []) {
    const ancestors = Object.freeze([...matched]);
    const section = matched[0] || null;
    return Object.freeze({
      catalogState: "not-found",
      currentCategory: null,
      categoryId: "",
      section,
      sectionId: section?.id || "all",
      ancestors,
      children: Object.freeze([]),
      descendantIds: Object.freeze([]),
      canonicalPath: rootPath,
      status: "not-found",
      notFound: true,
      reason,
      requestedPath: pathname,
      legacy: false,
      legacySourceCategoryId: "",
      redirectTo: ""
    });
  }

  function resolveCatalogPath(pathname) {
    const clean = normalizePathname(pathname);
    if (clean === rootPath) return categoryContext(null);
    if (!clean.startsWith(`${rootPath}/`)) return notFoundContext(clean, "outside-catalog");

    const segments = clean.slice(rootPath.length + 1).split("/").filter(Boolean);
    const matched = [];
    let parentId = null;
    for (const segment of segments) {
      const current = taxonomy.childrenOf(parentId).find(category => category.slug === segment);
      if (!current) return notFoundContext(clean, "invalid-hierarchy", matched);
      matched.push(current);
      parentId = current.id;
    }
    return matched.length ? categoryContext(matched.at(-1)) : categoryContext(null);
  }

  function legacyTarget(pathname, params, pageName) {
    const clean = normalizePathname(pathname);
    const querySection = legacyQuerySections[params.get("category")];
    if ((clean === rootPath || clean === "/catalog") && querySection) {
      return { categoryId: querySection, kind: "query-category", sourceCategoryId: "" };
    }

    const legacyPathCategory = legacyCategoryPaths[clean];
    if (legacyPathCategory) return { categoryId: legacyPathCategory, kind: "legacy-category-path", sourceCategoryId: "" };

    if (clean === "/catalog/water-treatment" && params.has("type")) {
      const sourceCategory = params.get("type");
      const mapped = sourceMappings?.categoryMappings?.ecosoft?.[sourceCategory];
      if (mapped?.categoryId) return { categoryId: mapped.categoryId, kind: "query-type", sourceCategoryId: mapped.categoryId };
    }

    const oldWaterMatch = clean.match(/^\/catalog\/water-supply\/water-treatment\/([^/]+)$/);
    if (oldWaterMatch) {
      const category = taxonomy.descendantsOf("water-treatment").find(item => item.slug === oldWaterMatch[1] || item.id === oldWaterMatch[1]);
      if (category) return { categoryId: category.id, kind: "legacy-water-path", sourceCategoryId: category.id };
    }

    const sectionAlias = legacySectionAliases[clean];
    if (sectionAlias) return { categoryId: sectionAlias, kind: "legacy-section", sourceCategoryId: "" };
    if (clean === rootPath && taxonomy.byId[pageName]?.level === 1) {
      return { categoryId: pageName, kind: "physical-page", sourceCategoryId: "" };
    }
    return null;
  }

  function resolveLocation(pathname = location.pathname, search = location.search, pageName = "catalog") {
    const params = new URLSearchParams(search || "");
    const clean = normalizePathname(pathname);
    const target = legacyTarget(clean, params, pageName);
    if (target) {
      const category = taxonomy.byId[target.categoryId];
      if (!category) return notFoundContext(clean, "unknown-legacy-target");
      const canonicalPath = getCategoryPath(category.id);
      return categoryContext(category, {
        legacy: true,
        legacyKind: target.kind,
        legacySourceCategoryId: target.sourceCategoryId,
        redirectTo: canonicalPath
      });
    }

    const resolved = resolveCatalogPath(clean);
    const usedHtmlExtension = /\.html(?:\/)?$/i.test(String(pathname || ""));
    if (usedHtmlExtension && !resolved.notFound) {
      return Object.freeze({ ...resolved, legacy: true, legacyKind: "html-extension", redirectTo: resolved.canonicalPath });
    }
    return resolved;
  }

  window.sofievkaCatalogRouting = Object.freeze({
    rootPath,
    normalizePathname,
    getCategoryAncestors,
    getCategoryPath,
    resolveCatalogPath,
    resolveLocation
  });
})();
/* Source: catalog/catalog-facade.js */
(function () {
  "use strict";

  const taxonomy = window.sofievkaTaxonomy;
  const attributeSchema = window.sofievkaAttributeSchema;
  const sourceMappings = window.sofievkaSourceMappings;
  const routing = window.sofievkaCatalogRouting;
  const products = Array.isArray(window.sofievkaNormalizedProducts) ? window.sofievkaNormalizedProducts : [];
  const brands = Array.isArray(window.sofievkaBrands) ? window.sofievkaBrands : [];
  if (!taxonomy || !attributeSchema || !sourceMappings || !routing) throw new Error("Central catalog modules are incomplete.");

  const catalogState = Object.freeze({
    id: "all",
    slug: "",
    name: "Усі товари",
    title: "Каталог обладнання",
    shortTitle: "Увесь каталог",
    description: "Інженерне обладнання для опалення, водопостачання, водоочищення, автоматизації, клімату та господарства.",
    menuDescription: "Усі товари та фільтри в одному каталозі",
    order: 0,
    status: "state",
    metaTitle: "Каталог інженерного обладнання | ТД «Софіївка»"
  });
  const sections = taxonomy.businessSections;
  const categories = taxonomy.productCategories;
  const sectionById = Object.freeze({ all: catalogState, ...Object.fromEntries(sections.map(section => [section.id, section])) });
  const categoryById = taxonomy.byId;
  const attributeDefinitions = attributeSchema.definitions;
  const catalogProducts = Object.freeze(products.filter(product => {
    const category = categoryById[product.primaryCategoryId];
    return category?.status === "active" && category?.visibility === "catalog";
  }));
  const serviceItems = Object.freeze(products.filter(product => categoryById[product.primaryCategoryId]?.visibility === "service"));

  function sectionUrl(sectionId = "all") {
    return sectionId === "all" ? routing.rootPath : routing.getCategoryPath(sectionId);
  }

  function categoryUrl(categoryId) {
    return routing.getCategoryPath(categoryId);
  }

  function brandUrl(brandId) { return `/brands/${encodeURIComponent(brandId)}`; }
  function descendantIds(categoryId) { return new Set([categoryId, ...taxonomy.descendantsOf(categoryId).map(category => category.id)]); }
  function productsForSection(sectionId) { return sectionId === "all" ? [...catalogProducts] : catalogProducts.filter(product => product.sectionId === sectionId); }
  function productsForCategory(categoryId) {
    const ids = descendantIds(categoryId);
    return catalogProducts.filter(product => ids.has(product.primaryCategoryId));
  }
  function availableCategories(sectionId) {
    return taxonomy.childrenOf(sectionId).filter(category => category.status === "active" && productsForCategory(category.id).length > 0);
  }
  const activeSections = Object.freeze(sections.filter(section => section.status === "active" && productsForSection(section.id).length > 0));
  const navigationSections = Object.freeze([catalogState, ...activeSections]);

  function resolveRoute(pathname = location.pathname, search = location.search, pageName = "catalog") {
    return routing.resolveLocation(pathname, search, pageName);
  }

  function valueLabel(definition, value) {
    if (attributeSchema.valueLabels[value]) return attributeSchema.valueLabels[value];
    if (definition?.type === "number") return `${new Intl.NumberFormat("uk-UA", { maximumFractionDigits: 4 }).format(Number(value))} ${definition.unit}`;
    return String(value);
  }

  const waterCategories = taxonomy.descendantsOf("water-treatment").filter(category => category.legacyGroup);
  const waterGroups = [...new Set(waterCategories.map(category => category.legacyGroup))].map(groupId => Object.freeze({
    slug: groupId,
    name: waterCategories.find(category => category.legacyGroup === groupId)?.legacyGroupTitle || groupId,
    children: Object.freeze(waterCategories.filter(category => category.legacyGroup === groupId).map(category => Object.freeze({ slug: category.slug, name: category.title })))
  }));
  const waterCategoryBySlug = Object.freeze(Object.fromEntries(waterCategories.map(category => [category.slug, Object.freeze({
    slug: category.slug,
    name: category.title,
    groupSlug: category.legacyGroup,
    groupName: category.legacyGroupTitle
  })])));
  const waterProducts = Object.freeze(catalogProducts.filter(product => product.sectionId === "water-treatment"));
  const distribution = Object.freeze(Object.fromEntries(waterCategories.map(category => [category.id, productsForCategory(category.id).length])));
  window.sofievkaWaterCatalog = Object.freeze({
    taxonomy: Object.freeze(waterGroups),
    categoryBySlug: waterCategoryBySlug,
    supplierMappings: sourceMappings.supplierLabelMappings.ecosoft,
    products: waterProducts,
    report: Object.freeze({
      sourceCount: window.sofievkaNormalizationReport?.waterSourceCount || waterProducts.length,
      normalizedCount: waterProducts.length,
      uncategorized: Object.freeze(waterProducts.filter(product => !categoryById[product.primaryCategoryId]).map(product => product.id)),
      duplicateIds: window.sofievkaNormalizationReport?.duplicateInputIds || Object.freeze([]),
      ambiguous: Object.freeze([]),
      distribution
    })
  });
  window.sofievkaProducts = waterProducts;

  window.sofievkaCatalog = Object.freeze({
    catalogState,
    sections,
    activeSections,
    navigationSections,
    categories,
    taxonomy,
    sectionById,
    categoryById,
    attributeDefinitions,
    attributeSchema,
    sourceMappings,
    products,
    catalogProducts,
    serviceItems,
    brands,
    slugify: window.sofievkaProductNormalizer.slugify,
    sectionUrl,
    categoryUrl,
    getCategoryPath: routing.getCategoryPath,
    getCategoryAncestors: routing.getCategoryAncestors,
    brandUrl,
    productsForSection,
    productsForCategory,
    availableCategories,
    resolveRoute,
    valueLabel
  });
})();
/* Source: catalog/pdp-engine.js */
(function () {
  "use strict";

  const catalog = window.sofievkaCatalog;
  if (!catalog) throw new Error("Catalog facade must load before the PDP engine.");

  const schema = catalog.attributeSchema || {};
  const definitions = catalog.attributeDefinitions || {};
  const products = catalog.products || [];
  const brands = catalog.brands || [];
  const groupDefinitions = Object.freeze([
    Object.freeze({ id: "main", title: "Основні параметри", keys: Object.freeze(["productType", "purpose", "type", "technology", "waterSource", "installation", "flowType", "scope", "pump", "mineralizer"]) }),
    Object.freeze({ id: "performance", title: "Робочі параметри", keys: Object.freeze(["capacityLh", "flowM3h", "headM", "kvs", "powerKw", "heatOutputKw", "pressureBar", "temperature", "filtrationMicron", "eei", "maxImmersionDepthM", "freePassageMm"]) }),
    Object.freeze({ id: "connection", title: "Підключення та сумісність", keys: Object.freeze(["diameter", "diameterMm", "connection", "mountingLengthMm", "outlets", "compatibility", "control", "voltage", "protectionClass", "material", "selfPriming", "cableLengthM", "floatSwitch"]) }),
    Object.freeze({ id: "dimensions", title: "Габарити та формат", keys: Object.freeze(["widthMm", "heightMm", "depthMm", "dimensions", "volumeL", "weightKg", "format"]) })
  ]);

  const hasValue = value => value !== undefined && value !== null && value !== "";
  const genericSourceLabels = new Set(["категорія", "бренд", "стан", "наявність"]);
  const cleanSourceLabel = label => String(label || "").replace(/\s+/g, " ").trim();
  const splitSourceLabel = label => {
    const normalized = cleanSourceLabel(label);
    const grouped = normalized.match(/^(Вимоги до води[^:]*|Допустимі показники[^:]*|Основні властивості|Рекомендовані умови експлуатації|Технічні параметри|Технічні характеристики(?: матеріалу)?|Умови експлуатації|Характеристики (?:градієнтного картриджа|картриджа|кварцового піску|колби фільтра|матеріалу)[^:]*|Яку воду може очистити фільтр\?|Якість води[^:]*?)\*{0,3}:\s*(.+)$/i);
    return grouped ? { group: grouped[1].trim(), label: grouped[2].trim() } : { group: "Основні характеристики", label: normalized };
  };
  const canonicalSourceLabel = label => splitSourceLabel(label).label.toLocaleLowerCase("uk-UA").replace(/[.*]+$/g, "").replace(/\s+/g, " ").trim();

  function sourceSpecificationEntries(product) {
    const candidates = (Array.isArray(product.sourceAttributes) ? product.sourceAttributes : [])
      .filter(item => item?.label && hasValue(item.value) && !genericSourceLabels.has(cleanSourceLabel(item.label).toLocaleLowerCase("uk-UA")))
      .map((item, index) => {
        const parsed = splitSourceLabel(item.label);
        return { id: `source-${index}`, group: parsed.group, label: parsed.label, value: String(item.value).trim(), prefixed: parsed.group !== "Основні характеристики", canonicalLabel: canonicalSourceLabel(item.label) };
      });
    const groupedDuplicates = new Set(candidates.filter(item => item.prefixed).map(item => `${item.canonicalLabel}\u0000${item.value}`));
    const chosen = new Map();
    candidates.forEach(candidate => {
        if (!candidate.prefixed && groupedDuplicates.has(`${candidate.canonicalLabel}\u0000${candidate.value}`)) return;
        const key = `${candidate.group.toLocaleLowerCase("uk-UA")}\u0000${candidate.canonicalLabel}`;
        const current = chosen.get(key);
        if (!current || candidate.value.length > current.value.length) chosen.set(key, candidate);
      });
    return [...chosen.values()];
  }

  const keyPriorities = Object.freeze({
    "reverse-osmosis": [/номінальна продуктивність|продуктивність/, /накопичувальний бак/, /тиск на вході/, /кількість ступенів/, /монтаж/, /конверсія/, /температура води на вході|температура вхідної води/],
    "flow-filters": [/кількість ступенів/, /продуктивність/, /тиск/, /температура/, /підключення/],
    "mechanical-treatment": [/продуктивність робоча/, /робочий тиск/, /діаметр підключення|підключення установки/, /рейтинг фільтрації|тонкість фільтрації/, /ресурс/],
    "water-softening": [/продуктивність робоча/, /робочий тиск/, /об'єм фільтрувального матеріалу/, /витрата солі/, /діаметр підключення|підключення установки/],
    "complex-treatment": [/продуктивність робоча/, /робочий тиск/, /об'єм фільтрувального матеріалу/, /ресурс/, /діаметр підключення|підключення установки/],
    "chlorine-odor-removal": [/продуктивність робоча/, /робочий тиск/, /об'єм фільтрувального матеріалу/, /ресурс/, /діаметр підключення|підключення установки/],
    "mainline-filters-housings": [/рейтинг фільтрації|тонкість фільтрації/, /типорозмір/, /діаметр підключення|підключення/, /робочий тиск/, /температура води/],
    "drinking-system-cartridges": [/рекомендований інтервал заміни/, /тонкість фільтрації/, /кількість ступенів|кількість елементів/, /тип води/, /сумісність/],
    "mainline-cartridges": [/рейтинг фільтрації|тонкість фільтрації/, /тип картриджа/, /матеріал/, /типорозмір/, /ресурс/, /температура води/],
    "filter-media": [/маса|вага/, /об'єм/, /упаковка/, /робоча температура/, /робочий діапазон ph/, /застосування/]
  });
  const attributeEntry = (product, id) => {
    const value = product.normalizedAttributes?.[id];
    const definition = definitions[id];
    if (!definition || !hasValue(value)) return null;
    return Object.freeze({ id, label: definition.label, value: catalog.valueLabel(definition, value) });
  };

  function keySpecs(product, limit = 5) {
    if (product.source?.supplier === "ecosoft") {
      const entries = sourceSpecificationEntries(product);
      const priorities = keyPriorities[product.primaryCategoryId] || [];
      const selected = [];
      priorities.forEach(pattern => {
        const match = entries.find(item => pattern.test(item.label.toLocaleLowerCase("uk-UA")) && !selected.includes(item));
        if (match) selected.push(match);
      });
      entries.forEach(item => { if (selected.length < limit && !selected.includes(item)) selected.push(item); });
      return Object.freeze(selected.slice(0, limit).map((item, index) => Object.freeze({ id: `source-key-${index}`, label: item.label, value: item.value })));
    }
    const priority = schema.pdpPriorityByCategory?.[product.primaryCategoryId] || [];
    const fallback = schema.pdpFallbackPriority || Object.keys(definitions);
    return Object.freeze([...new Set([...priority, ...fallback])].flatMap(id => attributeEntry(product, id) || []).slice(0, limit));
  }

  function specificationGroups(product) {
    if (product.source?.supplier === "ecosoft") {
      const grouped = new Map();
      sourceSpecificationEntries(product).forEach(item => {
        if (!grouped.has(item.group)) grouped.set(item.group, []);
        grouped.get(item.group).push(Object.freeze({ id: item.id, label: item.label, value: item.value }));
      });
      return Object.freeze([...grouped.entries()].map(([title, items], index) => Object.freeze({ id: `source-group-${index}`, title, items: Object.freeze(items) })));
    }
    const normalizedIds = Object.keys(product.normalizedAttributes || {}).filter(id => definitions[id] && hasValue(product.normalizedAttributes[id]));
    const unmappedItems = (Array.isArray(product.unmappedAttributes) ? product.unmappedAttributes : [])
      .filter(item => item?.label && hasValue(item.value))
      .map(item => Object.freeze({ id: `source-${item.label}`, label: item.label, value: String(item.value) }));
    if (normalizedIds.length <= 5) {
      const items = normalizedIds.map(id => attributeEntry(product, id)).filter(Boolean);
      const groups = items.length ? [Object.freeze({ id: "technical", title: "Технічні дані", items: Object.freeze(items) })] : [];
      if (unmappedItems.length) groups.push(Object.freeze({ id: "complete", title: "Повні характеристики", items: Object.freeze(unmappedItems) }));
      return Object.freeze(groups);
    }
    const assigned = new Set();
    const groups = groupDefinitions.flatMap(group => {
      const ids = group.keys.filter(id => normalizedIds.includes(id));
      const items = ids.map(id => attributeEntry(product, id)).filter(Boolean);
      if (items.length < 2) return [];
      ids.forEach(id => assigned.add(id));
      return [Object.freeze({ id: group.id, title: group.title, items: Object.freeze(items) })];
    });
    const remaining = normalizedIds.filter(id => !assigned.has(id)).map(id => attributeEntry(product, id)).filter(Boolean);
    if (remaining.length) groups.push(Object.freeze({ id: "other", title: "Інші характеристики", items: Object.freeze(remaining) }));
    if (unmappedItems.length) groups.push(Object.freeze({ id: "complete", title: "Повні характеристики", items: Object.freeze(unmappedItems) }));
    return Object.freeze(groups);
  }

  function images(product) {
    const candidates = [product.image, ...(Array.isArray(product.images) ? product.images : []), ...(Array.isArray(product.gallery) ? product.gallery : [])]
      .map(item => typeof item === "string" ? item : item?.src)
      .filter(Boolean);
    return Object.freeze([...new Set(candidates)]);
  }

  function documents(product) {
    return Object.freeze((Array.isArray(product.documents) ? product.documents : []).filter(item => item?.url && /^https?:\/\/|^\//.test(item.url)).map(item => Object.freeze({
      type: item.type || "document",
      title: item.title || "Документ виробника",
      url: item.url
    })));
  }

  function model(product) {
    const sourceModel = (product.unmappedAttributes || []).find(item => /^модель$/i.test(item.label) && hasValue(item.value))?.value;
    if (sourceModel) return String(sourceModel);
    if (product.model && product.model !== product.title) return String(product.model);
    return String(product.sku || product.id);
  }

  function purchase(product) {
    const status = product.inventory?.status || product.stockStatus || product.availability || "unknown";
    const amount = Number(product.pricing?.amount ?? product.price);
    const oldAmount = Number(product.pricing?.oldAmount ?? product.oldPrice ?? 0);
    return Object.freeze({
      status,
      statusLabel: product.availabilityLabel || (status === "in_stock" ? "В наявності" : status === "out_of_stock" ? "Немає в наявності" : "Наявність уточнюйте"),
      amount: Number.isFinite(amount) && amount > 0 ? amount : null,
      oldAmount: Number.isFinite(oldAmount) && oldAmount > amount ? oldAmount : null,
      purchasable: status === "in_stock" && Number.isFinite(amount) && amount > 0
    });
  }

  function relatedProducts(product, limit = 4) {
    const sourceAttributes = product.normalizedAttributes || {};
    return Object.freeze(products.filter(item => item.id !== product.id && item.primaryCategoryId === product.primaryCategoryId).map(item => {
      const shared = Object.keys(sourceAttributes).filter(id => hasValue(sourceAttributes[id]) && sourceAttributes[id] === item.normalizedAttributes?.[id]).length;
      const score = (item.seriesId && item.seriesId === product.seriesId ? 40 : 0) + (item.brandId === product.brandId ? 12 : 0) + shared * 5;
      const priceDistance = Math.abs(Number(item.price || 0) - Number(product.price || 0));
      return { item, score, priceDistance };
    }).sort((a, b) => b.score - a.score || a.priceDistance - b.priceDistance || a.item.id.localeCompare(b.item.id)).slice(0, limit).map(entry => entry.item));
  }

  function brand(product) {
    return brands.find(item => item.id === product.brandId) || null;
  }

  function installationRelevant(product) {
    return !new Set(["drinking-system-cartridges", "mainline-cartridges", "filter-media"]).has(product.primaryCategoryId);
  }

  window.sofievkaPdp = Object.freeze({ keySpecs, specificationGroups, images, documents, model, purchase, relatedProducts, brand, installationRelevant });
})();
/* Source: catalog/search-engine.js */
(function () {
  "use strict";

  const catalog = window.sofievkaCatalog;
  if (!catalog) throw new Error("Catalog search requires the centralized catalog facade.");
  const searchableProducts = catalog.catalogProducts || catalog.products;

  const SEARCHABLE_ATTRIBUTE_IDS = Object.freeze([
    "diameter", "diameterMm", "connection", "powerKw", "headM", "flowM3h", "filtrationMicron",
    "compatibility", "kvs", "mountingLengthMm", "pressureBar", "heatOutputKw", "outlets", "voltage",
    "control", "protectionClass", "temperature", "capacityLh", "format", "eei", "selfPriming",
    "maxImmersionDepthM", "freePassageMm", "cableLengthM", "floatSwitch", "volumeL", "material", "installation",
    "capacityKgh", "rotationRpm", "communication", "zones"
  ]);

  const CATEGORY_ALIASES = Object.freeze({
    "circulation-pumps": ["циркуляційний насос", "циркуляційні насоси", "циркуляционный насос", "циркуляционные насосы"],
    "water-pumps": ["насоси для водопостачання", "водяні насоси", "насосы водоснабжения"],
    "multistage-pumps": ["багатоступеневий насос", "багатоступеневі насоси", "многоступенчатый насос"],
    "drainage-pumps": ["дренажний насос", "дренажні насоси", "занурювальний насос", "дренажный насос"],
    "underfloor-heating": ["тепла підлога", "теплий пол", "теплый пол", "підігрів підлоги"],
    "distribution-hydraulics": ["гідравліка", "гидравлика", "колектор", "колектори", "коллектор", "насосна група"],
    automation: ["автоматика", "керування", "управління", "управление", "термостат", "контролер"],
    "heating-valves": ["арматура", "клапан", "клапани", "кран", "регулююча арматура"],
    "heating-components": ["комплектуючі", "комплектующие", "модульна система"],
    "water-treatment": ["водоочищення", "очищення води", "очистка воды", "фільтрація води"],
    "reverse-osmosis": ["осмос", "зворотний осмос", "зворотній осмос", "обратный осмос", "reverse osmosis"],
    "drinking-system-cartridges": ["картридж", "картриджі", "картриджи", "змінний елемент"],
    "mainline-cartridges": ["магістральний картридж", "магістральні картриджі", "картридж", "картриджі", "картриджи"],
    "mainline-filters-housings": ["магістральний фільтр", "корпус фільтра", "колба фільтра"],
    "filter-media": ["фільтрувальні матеріали", "фильтрующие материалы", "завантаження для фільтра", "засипка"],
    "complex-treatment": ["комплексне очищення", "комплексная очистка"],
    "water-softening": ["пом'якшення", "пом’якшення", "умягчение", "пом'якшувач"],
    "chlorine-odor-removal": ["видалення хлору", "хлор і запах", "удаление хлора"],
    "mechanical-treatment": ["механічне очищення", "механическая очистка"],
    "flow-filters": ["проточний фільтр", "проточні фільтри", "проточный фильтр"],
    "uv-disinfection": ["uv лампа", "uv-c лампа", "уф лампа", "знезараження води", "ультрафіолетова лампа"],
    "smart-home": ["розумний будинок", "умный дом", "sinum", "автоматизація будинку"],
    "smart-lighting": ["розумне освітлення", "сенсорний вимикач", "димер", "диммер"],
    "smart-sensors": ["датчик протікання", "датчик диму", "мультисенсор", "якість повітря"],
    "water-supply-components": ["комплектуючі для водопостачання", "баки та автоматика", "аксесуари насосів"],
    "pressure-tanks": ["мембранний бак", "гідроакумулятор", "напірний бак", "расширительный бак"],
    "pump-automation": ["автоматика насоса", "контролер насоса", "реле тиску", "pressure manager"],
    "pump-accessories": ["комплектуючі насоса", "аксесуари насоса", "монтажний комплект"],
    "pump-services": ["сервіс насосів", "пусконалагодження", "введення в експлуатацію"],
    "pool-pumps-filtration": ["насос для басейну", "фільтр для басейну", "фільтрувальна станція", "оборудование для бассейна"],
    "feed-grinders": ["кормоподрібнювач", "зерноподрібнювач", "корморізка", "кормоизмельчитель"]
  });

  const WORD_GROUPS = Object.freeze([
    ["насос", "насоси", "насосы", "помпа", "помпи", "помпы"],
    ["картридж", "картриджі", "картриджи"],
    ["фільтр", "фільтри", "фильтр", "фильтры"],
    ["колектор", "колектори", "коллектор", "коллекторы"],
    ["клапан", "клапани", "клапаны"],
    ["керування", "управління", "управление"],
    ["термостат", "терморегулятор", "регулятор температури"],
    ["бездротовий", "бездротове", "wireless"],
    ["дротовий", "дротове", "wired"],
    ["циркуляційний", "циркуляційні", "циркуляционный", "циркуляционные"],
    ["зворотний", "зворотній", "обратный"],
    ["матеріал", "матеріали", "материал", "материалы"],
    ["фільтрувальний", "фільтрувальні", "фильтрующий", "фильтрующие"]
  ]);

  const STOP_WORDS = new Set(["і", "й", "та", "для", "з", "зі", "із", "в", "у", "на", "по", "и", "для"]);
  const SERIES_LABELS = catalog.attributeSchema.seriesLabels || {};

  function normalizeQuery(value) {
    return String(value ?? "")
      .normalize("NFKC")
      .replace(/[’‘`´]/g, "'")
      .replace(/\s+/g, " ")
      .trim()
      .toLocaleLowerCase("uk");
  }

  function tokenize(value) {
    return normalizeQuery(value).match(/[a-zа-яіїєґ0-9]+(?:[./-][a-zа-яіїєґ0-9]+)*/giu) || [];
  }

  const normalizedWordGroups = WORD_GROUPS.map(group => group.map(normalizeQuery));
  function variantsForToken(token) {
    const group = normalizedWordGroups.find(items => items.includes(token));
    return group || [token];
  }

  function unique(values) {
    return [...new Set(values.filter(Boolean))];
  }

  function phraseWithTokens(values) {
    const phrases = unique(values.map(normalizeQuery));
    return Object.freeze({ phrases: Object.freeze(phrases), tokens: Object.freeze(unique(phrases.flatMap(tokenize))) });
  }

  function technicalAttributeText(product) {
    const values = [];
    SEARCHABLE_ATTRIBUTE_IDS.forEach(id => {
      const value = product.normalizedAttributes?.[id];
      if (value === undefined || value === null || value === "") return;
      const definition = catalog.attributeDefinitions[id];
      const display = catalog.valueLabel(definition, value);
      values.push(id, definition?.label || "", display);
      if (/\d\s*["″]/.test(String(display))) values.push(`${String(display).replace(/["″]/g, "")} дюйм`, "inch");
    });
    return values;
  }

  function collectionText(product) {
    const collections = [
      ...(Array.isArray(product.collections) ? product.collections : []),
      ...(Array.isArray(product.tags) ? product.tags : [])
    ];
    return collections.flatMap(collection => {
      if (/sale|discount/i.test(collection)) return [collection, "sale", "розпродаж", "знижка"];
      return [collection];
    });
  }

  function identifierVariants(value) {
    const normalized = normalizeQuery(value).replace(/\s+/g, "");
    const variants = [normalized];
    // Supplier codes occasionally confuse the letter O and zero. Keep this controlled
    // equivalence below exact identifier matches instead of applying general fuzzy SKU search.
    if (/[oо](?=\d)|(?<=\d)[oо]/iu.test(normalized)) variants.push(normalized.replace(/[oо]/giu, "0"));
    if (/0(?=\d)|(?<=\d)0/u.test(normalized)) variants.push(normalized.replace(/0/g, "o"));
    return unique(variants);
  }

  const categoryIndex = Object.freeze(catalog.categories
    .filter(category => category.status === "active")
    .map(category => {
      const products = catalog.productsForCategory(category.id);
      const aliases = CATEGORY_ALIASES[category.id] || [];
      const parentNames = catalog.getCategoryAncestors(category.id).map(item => item.title || item.name);
      const text = phraseWithTokens([category.title, category.shortTitle, ...parentNames, ...aliases]);
      return Object.freeze({ type: "category", entity: category, count: products.length, href: catalog.getCategoryPath(category.id), aliases: Object.freeze(aliases), ...text });
    })
    .filter(item => item.count > 0));

  const productCountByBrand = searchableProducts.reduce((counts, product) => {
    counts[product.brandId] = (counts[product.brandId] || 0) + 1;
    return counts;
  }, {});
  const brandIndex = Object.freeze(catalog.brands.map(brand => {
    const aliases = Array.isArray(brand.aliases) ? brand.aliases : [];
    const text = phraseWithTokens([brand.name, brand.slug, ...aliases]);
    return Object.freeze({ type: "brand", entity: brand, count: productCountByBrand[brand.slug] || 0, href: catalog.brandUrl(brand.slug), aliases: Object.freeze(aliases), ...text });
  }));

  const productIndex = Object.freeze(searchableProducts.map(product => {
    const category = catalog.categoryById[product.primaryCategoryId];
    const categoryChain = category ? [...catalog.getCategoryAncestors(category.id), category] : [];
    const categoryAliases = categoryChain.flatMap(item => CATEGORY_ALIASES[item.id] || []);
    const seriesLabel = product.seriesId ? SERIES_LABELS[product.seriesId] || product.seriesId : "";
    const seriesName = product.seriesId ? `${product.brand} ${seriesLabel}` : "";
    const title = phraseWithTokens([product.title, product.shortTitle]);
    const model = phraseWithTokens([product.model]);
    const brand = phraseWithTokens([product.brand, product.brandId]);
    const series = phraseWithTokens([seriesName, seriesLabel, product.seriesId]);
    const categoryText = phraseWithTokens([...categoryChain.flatMap(item => [item.title, item.shortTitle]), ...categoryAliases]);
    const attributes = phraseWithTokens(technicalAttributeText(product));
    const collections = phraseWithTokens(collectionText(product));
    return Object.freeze({
      product,
      sku: normalizeQuery(product.sku).replace(/\s+/g, ""),
      skuVariants: Object.freeze(identifierVariants(product.sku)),
      title,
      model,
      brand,
      series,
      category: categoryText,
      attributes,
      collections
    });
  }));

  const seriesIndex = Object.freeze([...new Set(searchableProducts.map(product => product.seriesId).filter(Boolean))].map(seriesId => {
    const products = searchableProducts.filter(product => product.seriesId === seriesId);
    const brand = products[0]?.brand || "";
    const label = SERIES_LABELS[seriesId] || seriesId;
    const name = `${brand} ${label}`.trim();
    const text = phraseWithTokens([name, label, seriesId]);
    return Object.freeze({ type: "series", entity: Object.freeze({ id: seriesId, name, label, brand }), count: products.length, href: `/search?q=${encodeURIComponent(name)}`, aliases: Object.freeze([]), ...text });
  }));
  const fuzzyVocabulary = Object.freeze(unique([
    ...productIndex.flatMap(entry => [...entry.title.tokens, ...entry.brand.tokens, ...entry.category.tokens]),
    ...brandIndex.flatMap(entry => entry.tokens),
    ...categoryIndex.flatMap(entry => entry.tokens),
    ...seriesIndex.flatMap(entry => entry.tokens)
  ]));
  const fuzzyMatchCache = new Map();

  function levenshtein(first, second, maxDistance) {
    if (first === second) return 0;
    if (Math.abs(first.length - second.length) > maxDistance) return maxDistance + 1;
    let previous = Array.from({ length: second.length + 1 }, (_, index) => index);
    for (let i = 1; i <= first.length; i += 1) {
      const current = [i];
      let rowMinimum = i;
      for (let j = 1; j <= second.length; j += 1) {
        current[j] = Math.min(current[j - 1] + 1, previous[j] + 1, previous[j - 1] + (first[i - 1] === second[j - 1] ? 0 : 1));
        rowMinimum = Math.min(rowMinimum, current[j]);
      }
      if (rowMinimum > maxDistance) return maxDistance + 1;
      previous = current;
    }
    return previous[second.length];
  }

  function fuzzyTokenMatch(queryToken, candidateTokens) {
    if (!/^[a-zа-яіїєґ]+$/iu.test(queryToken) || queryToken.length < 4) return false;
    if (!fuzzyMatchCache.has(queryToken)) {
      const maxDistance = queryToken.length >= 8 ? 2 : 1;
      fuzzyMatchCache.set(queryToken, new Set(fuzzyVocabulary.filter(candidate => candidate.length >= 4 && levenshtein(queryToken, candidate, maxDistance) <= maxDistance)));
    }
    const matches = fuzzyMatchCache.get(queryToken);
    return candidateTokens.some(candidate => matches.has(candidate));
  }

  function phraseMatchScore(query, field, scores) {
    if (!query || !field.phrases.length) return 0;
    if (field.phrases.includes(query)) return scores.exact;
    if (field.phrases.some(value => value.startsWith(query))) return scores.prefix;
    if (field.phrases.some(value => value.includes(query))) return scores.contains;
    return 0;
  }

  function tokenMatchScore(token, entry) {
    const variants = variantsForToken(token);
    const fields = [
      [entry.title, 155, 95],
      [entry.model, 145, 90],
      [entry.brand, 135, 85],
      [entry.series, 130, 82],
      [entry.category, 125, 78],
      [entry.attributes, 72, 44],
      [entry.collections, 64, 38]
    ];
    if (variants.some(value => entry.skuVariants.includes(value))) return 900;
    if (/\d/u.test(token) && variants.some(value => entry.skuVariants.some(sku => sku.startsWith(value)))) return 620;
    for (const [field, exactScore, containsScore] of fields) {
      if (variants.some(value => field.tokens.includes(value))) return exactScore;
      if (variants.some(value => field.phrases.some(phrase => phrase.includes(value)))) return containsScore;
    }
    const fuzzyTokens = unique([...entry.title.tokens, ...entry.brand.tokens, ...entry.category.tokens]);
    return fuzzyTokenMatch(token, fuzzyTokens) ? 24 : 0;
  }

  function scoreProduct(entry, normalizedQuery, tokens) {
    const compact = normalizedQuery.replace(/\s+/g, "");
    if (entry.sku === compact) return 100000;
    if (entry.model.phrases.includes(normalizedQuery)) return 90000;
    if (entry.title.phrases.includes(normalizedQuery)) return 85000;
    const hasNumber = tokens.some(token => /\d/u.test(token));
    const hasUnit = tokens.some(token => ["мм", "м", "мкм", "квт", "вт", "бар", "л", "дюйм", "inch"].includes(token));
    const technicalPairs = tokens.flatMap((token, index) => /\d/u.test(token) && tokens[index + 1] ? [`${token} ${tokens[index + 1]}`] : []);
    if (hasNumber && hasUnit && ![entry.title, entry.model, entry.attributes].some(field => field.phrases.some(phrase => technicalPairs.some(pair => phrase.includes(pair))))) return 0;
    let score = 0;
    if (entry.sku.startsWith(compact)) score += 18000;
    else if (entry.skuVariants.includes(compact)) score += 12000;
    score += phraseMatchScore(normalizedQuery, entry.title, { exact: 0, prefix: 8000, contains: 4200 });
    score += phraseMatchScore(normalizedQuery, entry.model, { exact: 0, prefix: 7200, contains: 3600 });
    score += phraseMatchScore(normalizedQuery, entry.brand, { exact: 3200, prefix: 1900, contains: 1100 });
    score += phraseMatchScore(normalizedQuery, entry.series, { exact: 3000, prefix: 1700, contains: 1000 });
    score += phraseMatchScore(normalizedQuery, entry.category, { exact: 2800, prefix: 1500, contains: 900 });
    score += phraseMatchScore(normalizedQuery, entry.attributes, { exact: 0, prefix: 260, contains: 180 });
    score += phraseMatchScore(normalizedQuery, entry.collections, { exact: 500, prefix: 360, contains: 220 });

    let coverage = 0;
    let tokenScore = 0;
    tokens.forEach(token => {
      const matchScore = tokenMatchScore(token, entry);
      if (matchScore > 0) coverage += 1;
      tokenScore += matchScore;
      const variants = variantsForToken(token);
      if (variants.some(value => entry.category.tokens.includes(value))) tokenScore += 175;
      if (variants.some(value => entry.brand.tokens.includes(value))) tokenScore += 145;
      if (variants.some(value => entry.series.tokens.includes(value))) tokenScore += 135;
    });
    if (coverage !== tokens.length) return 0;
    score += tokenScore + (tokens.length > 1 ? coverage * 210 : 0);
    return score;
  }

  function scoreEntity(item, normalizedQuery, tokens) {
    const exact = item.phrases.includes(normalizedQuery);
    if (exact) return 10000;
    const prefix = item.phrases.some(value => value.startsWith(normalizedQuery));
    if (prefix) return 4800;
    const contains = item.phrases.some(value => value.includes(normalizedQuery));
    if (contains) return 2600;
    let coverage = 0;
    let score = 0;
    tokens.forEach(token => {
      const variants = variantsForToken(token);
      if (variants.some(value => item.tokens.includes(value))) { coverage += 1; score += 240; return; }
      if (fuzzyTokenMatch(token, item.tokens)) { coverage += 1; score += 45; }
    });
    return coverage === tokens.length ? score : 0;
  }

  function meaningfulTokens(query) {
    const all = tokenize(query);
    const filtered = all.filter(token => !STOP_WORDS.has(token));
    return filtered.length ? filtered : all;
  }

  function rankedEntities(items, normalizedQuery, tokens, limit, filter = () => true) {
    return items
      .filter(filter)
      .map(item => ({ ...item, score: scoreEntity(item, normalizedQuery, tokens) }))
      .filter(item => item.score > 0)
      .sort((first, second) => second.score - first.score || second.count - first.count || first.entity.name?.localeCompare(second.entity.name, "uk"))
      .slice(0, limit);
  }

  function search(query, options = {}) {
    const normalizedQuery = normalizeQuery(query);
    const tokens = meaningfulTokens(normalizedQuery);
    const limits = {
      products: Number.isFinite(options.productLimit) ? options.productLimit : Infinity,
      categories: Number.isFinite(options.categoryLimit) ? options.categoryLimit : Infinity,
      brands: Number.isFinite(options.brandLimit) ? options.brandLimit : Infinity,
      series: Number.isFinite(options.seriesLimit) ? options.seriesLimit : Infinity
    };
    if (!normalizedQuery || !tokens.length) return Object.freeze({ query: String(query ?? ""), normalizedQuery, totalProducts: 0, products: Object.freeze([]), productHits: Object.freeze([]), categories: Object.freeze([]), brands: Object.freeze([]), series: Object.freeze([]) });

    const allProductHits = productIndex
      .map(entry => ({ product: entry.product, score: scoreProduct(entry, normalizedQuery, tokens) }))
      .filter(hit => hit.score > 0)
      .sort((first, second) => second.score - first.score || first.product.title.localeCompare(second.product.title, "uk"));
    const exactBrandQuery = brandIndex.some(item => item.phrases.includes(normalizedQuery));
    const brands = rankedEntities(brandIndex, normalizedQuery, tokens, limits.brands, item => item.count > 0 || (exactBrandQuery && item.phrases.includes(normalizedQuery)));
    const categories = rankedEntities(categoryIndex, normalizedQuery, tokens, limits.categories);
    const series = rankedEntities(seriesIndex, normalizedQuery, tokens, limits.series);
    const productHits = allProductHits.slice(0, limits.products);
    return Object.freeze({
      query: String(query ?? ""),
      normalizedQuery,
      tokens: Object.freeze(tokens),
      totalProducts: allProductHits.length,
      products: Object.freeze(productHits.map(hit => hit.product)),
      productHits: Object.freeze(productHits),
      categories: Object.freeze(categories),
      brands: Object.freeze(brands),
      series: Object.freeze(series)
    });
  }

  const api = Object.freeze({
    search,
    normalizeQuery,
    tokenize,
    searchableAttributeIds: SEARCHABLE_ATTRIBUTE_IDS,
    aliases: CATEGORY_ALIASES,
    index: Object.freeze({ products: productIndex, brands: brandIndex, categories: categoryIndex, series: seriesIndex })
  });
  window.sofievkaCatalogSearch = api;
  window.catalogSearch = search;
})();
